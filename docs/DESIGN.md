# Cuprum — дизайн-система и решения

Источник правды для внешнего вида UI. Любой новый экран обязан использовать токены и
компоненты отсюда, а не сырые палитры Tailwind. Появился после рассинхрона: первый
вариант Home был синий с нативным `<select>`, тогда как редактор засветки — тёмный с
медным акцентом и аккуратными контролами. Чтобы такого не повторялось — этот файл.

## Тема

Тёмная техническая тема (вайб VSCode / BambuLab) с **медным (copper) акцентом**.

**Единый источник темы — `cuprum-ui/src/styles.css`** (Tailwind v4 `@theme inline`): HSL-токены
заданы там как CSS-переменные и проброшены в утилиты Tailwind. Меняем цвета только там;
таблица ниже — снимок для справки, а не второй источник. В разметке используем
**семантические классы** (`bg-primary`, `text-muted-foreground`, …), а НЕ сырые
(`bg-blue-600`, `text-neutral-400`).

### Цветовые токены

| Токен (Tailwind) | Назначение | HSL |
|---|---|---|
| `bg-background` / `text-foreground` | фон приложения / основной текст | `222 16% 9%` / `210 20% 92%` |
| `bg-card` | плавающие карточки/поповеры | `222 15% 12%` |
| `bg-panel` | боковые панели (инспектор, рейл-контейнеры) | `222 14% 13%` |
| `bg-muted` / `text-muted-foreground` | приглушённый фон / вторичный текст | `222 12% 18%` / `215 14% 60%` |
| `border-border` | разделители, рамки | `222 12% 20%` |
| `border-input` | рамки полей ввода | `222 12% 22%` |
| `ring-ring` | фокус-кольцо | = primary |
| `bg-primary` / `text-primary-foreground` | **медный акцент**: первичные кнопки, активные состояния | `24 80% 52%` / `24 30% 10%` |
| `bg-destructive` / `text-destructive-foreground` | опасные действия (Stop, удалить) | `0 70% 50%` / белый |
| `bg-pcb-preview` | плейсхолдер превью платы на Home (до 3D/preview) | `158 45% 14%` |
| `bg-info` / `text-info` | информационный тон (синий) — статус-пилюли ЧПУ (подвод/базирование) | `205 88% 56%` |
| `bg-field` | подложка рабочего поля ЧПУ (чуть темнее карточки) | `220 16% 8%` |
| `text-axis-x` / `bg-axis-x` | акцент оси X в DRO/поле ЧПУ (красный) | `6 78% 57%` |
| `text-axis-y` / `bg-axis-y` | акцент оси Y (зелёный) | `145 55% 50%` |
| `text-axis-z` / `bg-axis-z` | акцент оси Z (синий) | `205 88% 58%` |
| `--radius` | базовый радиус (`rounded-lg`=0.5rem, `md`, `sm`) | `0.5rem` |
| `--radius-xl` (`rounded-xl`) | крупный радиус для карточек экрана ЧПУ | `0.5rem + 4px` |

**Правило акцента:** всё «активное/первичное» — медное. Никакого синего. Первичная
кнопка — `bg-primary text-primary-foreground`. Активный сегмент/иконка-таб —
`bg-primary/20 text-primary` (см. рейл и тулбар). Фокус — `focus:ring-1 focus:ring-ring`.

**Исключение — технические цвета осей/статусов ЧПУ.** Токены `axis-x/y/z` и `info`
синего/зелёного спектра используются ТОЛЬКО на экране ручного управления ЧПУ: подписи
осей в DRO/рабочем поле и информационные статус-пилюли (подвод/базирование). Это
функциональная кодировка (оси X/Y/Z привычно красный/зелёный/синий), а не общий акцент —
в остальном UI правило «медное активное, синего нет» в силе.

## Типографика

- База: `13px`, `ui-sans-serif/system-ui` (см. `styles.css`).
- Заголовок секции: `text-[11px] font-semibold uppercase tracking-wide text-muted-foreground`.
- Числовые значения: `tabular-nums` (чтобы не «прыгали»).
- Вторичный текст/подписи: `text-muted-foreground`, размеры `text-[11px]/[12px]`.

## Поверхности и слои

- Корень приложения — `bg-background`.
- Боковые панели и контейнеры навигации — `bg-panel`, разделены `border-border`.
- Плавающие элементы над холстом (палитра инструментов) — `bg-card/90` + `shadow-lg` + `backdrop-blur`.
- Секции внутри панели — разделитель `border-b border-border`, паддинг `px-3 py-3`.

## Компоненты (реюзабельные)

Все интерактивные контролы — из `components/ui/`. **Не верстаем нативные `<select>`
и «голые» `<input>` по месту** — берём примитив.

### Существующие (`components/ui/`)
- **`Button`** (`Button.tsx`) — cva-варианты: `default` (медная), `secondary` (muted),
  `ghost`, `destructive`, `outline`; размеры `default`/`sm`/`icon`. Иконка lucide слева через `gap-2`.
- **`Slider`** (`Slider.tsx`) — radix; трек `bg-muted`, заполнение/палец `bg-primary`.
- **`Switch`** (`Switch.tsx`) — radix; вкл — `bg-primary`, выкл — `bg-muted`.
- **`HelpTip`** (`HelpTip.tsx`) — иконка-«?» (lucide `HelpCircle`) с поповер-подсказкой на
  radix-tooltip. Контент — `bg-popover`, опц. SVG-иллюстрация сверху. Ховер/курсор — только
  на самой иконке (`cursor-help`, `hover:bg-foreground/10`), не на строке. Ставим у полей
  настроек/форм, чтобы пояснить параметр.
- **`Popover`** (`Popover.tsx`) — radix-поповер: `Popover`/`PopoverTrigger`/`PopoverContent`.
  Контент — `rounded-md border border-border bg-popover p-1 shadow-lg`, портал, `z-50`. Для
  небольших закрываемых контролов у триггера (напр. фильтр типов проблем на превью).
- **`Checkbox`** (`Checkbox.tsx`) — radix-чекбокс (квадрат с галочкой lucide `Check`), вкл —
  `bg-primary`. Для множественного выбора независимых опций (в отличие от вкл/выкл `Switch`).
- **`ContextMenu`** (`ContextMenu.tsx`) — radix контекстное меню по правому клику:
  `ContextMenu`/`ContextMenuTrigger`(`disabled` пропускает нативное меню)/`ContextMenuContent`/
  `ContextMenuItem`/`ContextMenuCheckboxItem`/`ContextMenuLabel`/`ContextMenuSeparator`. Стиль
  контента как у `Popover`; пункт — `focus:bg-foreground/10`, галочка в левом жёлобе (`pl-7`).

### Примитивы форм (заведены в этой итерации)
- **`TextInput`** — текстовое поле/поиск. База поля:
  `h-8 rounded-md border border-input bg-background px-2 text-[12px] outline-none focus:ring-1 focus:ring-ring`.
  Вариант с иконкой-префиксом (например, лупа поиска) — иконка `absolute`, паддинг слева.
- **`Select`** — стилизованный селект (НЕ нативный вид): тот же бордюр/фон, что у полей,
  `h-8 rounded-md border border-input bg-background px-2 pr-7 text-[12px]`, своя стрелка
  (lucide `ChevronDown`, `absolute`, `pointer-events-none`), скрываем нативную стрелку
  (`appearance-none`). Опции — `bg-popover`.
- **`SegmentedControl`** — переключатель из 2–N иконок/лейблов (сетка/список и т.п.):
  контейнер `rounded-md border border-input overflow-hidden`, активный сегмент
  `bg-primary/20 text-primary`, неактивный `text-muted-foreground hover:text-foreground`.
  Опц. `tone` на сегменте (`danger`/`warning`/`success`) красит его независимо от
  активности — чтобы таб привлекал внимание (напр. красный таб feasibility при
  блокирующем DFM-вердикте), а не полагался на легко-пропускаемый кружок.
- **`NavTabs`** — «бамбу-стиль» таб-бар: НЕ склеенные сегменты (как `SegmentedControl`),
  а отдельные кнопки `rounded-lg px-3 py-1.5`, активная подсвечена фоном
  (`bg-primary/15 text-primary`), неактивная — `text-muted-foreground hover:text-foreground`.
  Опц. `icon`/`trailing` (нода, не компонент — вызывающий сам подставит спиннер при
  загрузке или точку-вердикт) и `tone` (`danger`/`warning`) тинтит активный и неактивный.
  Рендерит кнопки фрагментом, чтобы вставать в общий flex-ряд рядом с другими контролами
  (шестерёнка, кольцо прогресса). Общий для шапки проекта (`ProjectPage`) и инспектора
  дизайна (`DesignInspector`).
- **`EditableText`** — инлайн-редактируемый текст: лейбл-кнопка, по клику → `<input>`;
  Enter/blur коммитят обрезанное значение через `onCommit`, Escape (или пустое/
  неизменённое) — откат. `className` применяется и к лейблу, и к полю, чтобы
  типографика совпадала и раскладка не прыгала. Для переименования по месту без
  отдельного диалога.
- **`ProgressRing`** — определённый круговой прогресс (SVG-кольцо). Проп
  `value: 0..1` (клампится), цвет по `currentColor`, размер через `className`.
  Фоновое кольцо `strokeOpacity 0.2`, дуга `stroke-linecap round`, плавный
  `stroke-dashoffset`. Для прогресса подготовки артефактов на карточке дизайна.
- **`FormModal`** — `Modal` со стандартным футером формы: ghost-кнопка «Отмена» +
  основная «Сохранить» (выключена, пока `canSave=false`). Тело — `children` (поля,
  их состояние и логика сохранения — на вызывающем). Лейблы кнопок по умолчанию из
  `project:settings.{cancel,save}`, перекрываются `cancelLabel`/`saveLabel`. Для
  маленьких диалогов имя/описание (переименование дизайна, настройки проекта/недавнего).
- **`LabeledTextInput`** — подпись (`text-[11px] text-muted-foreground`) над `TextInput`;
  все пропы инпута проходят насквозь, `className` — на инпут (напр. `mb-3` между полями).
- **`PanelStatus`** — центрированное на всю высоту состояние превью-панели: опц. спиннер
  (`loading`) над опц. подписью (`message`); `spinnerClassName` (дефолт `size-5`, 3D — `size-6`),
  `className` для добавок (`w-full`). Для loading / «нет данных» во вкладках превью.
- **`VerdictDot`** — компактная точка-статус по DFM-вердикту (на базе `SEVERITY.dot`);
  `null` (ещё не посчитан) → muted. Размер/позицию даёт `className`. Компактный аналог
  `VerdictBadge` (угол карточки, строка пикера, иконка таба).
- **`DatumCornerPicker`** — сетка 2×2 для выбора угла рабочего нуля (датума): где у панели
  лежит станочный 0,0. Пропы `value: DatumCorner` / `onChange`; рисует только сетку (заголовок
  даёт вызывающий — `<p>` в инспекторе сверловки, заголовок `Card` в панели станка). Подписи
  углов — из ns `drill` (`datum.*`). Одно и то же состояние (`drillDatumCorner`) и из редактора
  сверловки, и из живой панели управления станком.

### Планируется
- **`NumberInput`** — числовое поле; вынести из `Inspector.NumberField`
  (`h-7 w-20 rounded-md border border-input bg-background px-2 text-right tabular-nums`,
  опц. `label`/`suffix`) при подключении редактора засветки, заодно отрефакторив Inspector.

### Иконки
- Только **lucide-react** (плоские SVG). Без эмодзи. Размер обычно `size-4` (16) в кнопках,
  20 в рейле навигации.
- Кнопка-иконка / таб: активная — `bg-primary/20 text-primary`, иначе
  `text-muted-foreground hover:text-foreground`, `disabled:opacity-30`.

### Курсоры
Курсор должен соответствовать семантике элемента — задаём явно, не полагаемся на дефолт.
- **Интерактивное (кнопки, табы, ссылки, кликабельные строки)** — `cursor-pointer`. Глобально
  уже включено для `button:not(:disabled)` в `styles.css`; для не-`button` кликабельного
  ставим класс явно.
- **Подсказка/справка** (иконка-«?», `HelpTip`, элемент с тултипом-пояснением) — `cursor-help`.
- **Заблокированное** — `cursor-not-allowed` + `opacity` (radix-контролы делают это сами через
  `disabled:`).
- **Перетаскивание** (раскладка плат, ручки) — `cursor-grab` / `cursor-grabbing`.
- **Ввод текста** — дефолтный `text` (не трогаем).
Ховер-эффект (цвет/подложка) вешаем на сам элемент, а не на строку-контейнер — чтобы реакция
была привязана к иконке/контролу, а не ко всей строке.

## Структура файлов фронтенда

Проект растёт — держим дерево чистым. **Один компонент = один файл.** Выделяем
переиспользуемые примитивы.

```
cuprum-ui/src/
  pages/                 # точки входа уровня экрана (рендерятся из App по view)
    HomePage.tsx
    ProjectPage.tsx
    PrinterPage.tsx
    SettingsPage.tsx
  components/
    ui/                  # реюзабельные примитивы (Button, Slider, Switch,
                         #   TextInput, NumberInput, Select, SegmentedControl)
    nav/                 # навигация (NavRail)
    home/                # части Home (RecentTile, …)
    editor/              # редактор засветки (PreviewCanvas, Inspector,
                         #   CanvasToolbar, StatusBar) — пока не смонтирован
    panel/               # вкладка Панель (PanelBlankCanvas, PanelToolPalette)
    project/             # проект: галерея/карточки/инспектор дизайнов (+ DesignPickerRow)
  windows/               # корни вторичных OS-окон (AddDesignWindow); монтируются
                         #   из main.tsx по getCurrentWindow().label
  hooks/                 # кросс-оконные/переиспользуемые хуки (useAddDesignBridge)
  lib/                   # api.ts, utils.ts (cn)
  shellStore.ts          # стор оболочки (навигация + недавние + текущий проект)
  store.ts               # стор редактора засветки
  App.tsx, main.tsx, styles.css
```

Правила:
- **`pages/`** — композиция экрана из компонентов; точка входа на каждый `view` оболочки.
- **`components/<группа>/`** — группируем по смыслу (`ui`, `nav`, `home`, `editor`).
- Переиспользуемое (поля, селекты, кнопки) живёт только в `components/ui/`.
- Все импорты — через алиас `@/` (не относительные), чтобы перенос файлов не ломал ссылки.
- (На будущее) сторы можно собрать в `src/stores/`; пока два стора лежат в корне `src/`.

## Журнал решений

- **2026-05-29 — медный акцент, семантические токены.** Акцент `--primary` = медь (`24 80% 52%`).
  В разметке только семантические классы; сырые палитры (`bg-blue-*`, `text-neutral-*`) запрещены —
  иначе экраны расходятся по стилю (как разъехались Home и редактор засветки в первой версии).
- **2026-05-29 — radix + cva примитивы.** Интерактивные контролы строим на radix (Switch/Slider)
  и cva-вариантах (Button); нативные `<select>` не используем — заводим `Select` с общим стилем поля.
- **2026-05-29 — иконки lucide-react, без эмодзи.**
- **2026-05-29 — структура `pages/` + сгруппированные `components/`,** один компонент на файл.
- **2026-05-30 — курсоры по семантике (см. «Курсоры»).** `pointer` для интерактива, `help` для
  подсказок (`HelpTip`/«?»), `not-allowed` для заблокированного, `grab`/`grabbing` для
  перетаскивания. Ховер вешаем на сам элемент, не на строку. Заведено, чтобы не повторять
  правило вручную для каждого нового контрола.
- **2026-05-30 — `HelpTip` + схематичные SVG-диаграммы (`components/settings/diagrams.tsx`)**
  для пояснения параметров (медь=золото, маска=зелёный, FR4=олива, размерные линии
  `currentColor`). Настройки — левое таб-меню (`SettingsPage`), расширяемое новыми группами.
- **2026-06-01 — страница дизайнов: галерея + карточки + per-Design инспектор.** Вкладка
  Designs = сетка карточек (по одной на Design: 2D-композит-thumbnail **без хрома** —
  без линеек/зума/бейджа размера, fit-to-view с маргином — + точка DFM-вердикта), карточка
  «+ Добавить дизайн» того же размера (грид-стретч строки), drag'n'drop ZIP прямо в каталог,
  удаление по hover-кнопке-корзине (оверлей, не вложенная в карточку-кнопку). Клик по карточке →
  переиспользуемый `DesignInspector` (2D/3D, метрики, feasibility, редактируемые типы слоёв).
  Thumbnail кликабелен → `cursor-pointer` на svg (svg не `button`, курсор задаём явно — см. «Курсоры»).
  `LayerStack` получил проп `chrome` для статичного thumbnail-режима.
- **2026-06-01 — `color-scheme: dark` в корне темы.** Объявлено в `:root, .dark` (styles.css).
  WKWebView (Tauri) без этого рисует нативные контролы в светлой схеме — спиннеры
  `<input type="number">` выходили чёрными стрелками на белой подложке поверх тёмного поля.
  Объявление перекрашивает все нативные form-контролы под тёмный UI (правит редактор панели и
  числовые поля Настроек). Карточка дизайна: реальный размер платы в правом нижнем углу футера
  (источник — `projectBoardMetrics`, формат — `useUnitFormat`). Тайлы недавних получили
  hover-кнопку-корзину «убрать из недавних» (тот же паттерн оверлея, что у карточки дизайна;
  убирает только из MRU, файл на диске не трогает).
- **2026-06-03 — «Добавить дизайн на панель»: отдельное OS-окно + палитра инструментов на холсте.**
  Вкладка Панель получила **плавающую палитру инструментов** (у левого края холста, `absolute left-3 top-3`,
  `bg-card/90 backdrop-blur shadow-lg`, вертикальная полоса, кнопки `size-9`): инструменты Выбрать/Панорама
  (активный — `bg-primary/20 text-primary`) и действия Добавить дизайн / Дублировать / Удалить (последние два
  пока `disabled`). Само добавление вынесено в **отдельное OS-окно** (Tauri `WebviewWindow`, лейбл `add-design`;
  один бандл, SPA ветвится в `main.tsx` по `getCurrentWindow().label`), а не модалку. Раскладка окна —
  двухколоночная: слева список дизайнов (`w-[320px]`, `bg-panel`, поиск + строки `DesignPickerRow` с
  превью-миниатюрой/размером/точкой-вердиктом + зона импорта ZIP внизу), справа — превью/детали (`flex-1`);
  drag-n-drop ZIP подсвечивается оверлеем `absolute inset-2 border-2 border-dashed border-primary`. Главное
  окно — единственный владелец состояния проекта; окно-«пульт» шлёт интенты (импорт / добавление) через события
  Tauri и получает снимок. Размещённые платы рисуются на Konva-холсте контуром в медном акценте (read-only).
  Схематичный рендер превью (линиями цветов слоёв, без паяльной маски) и авторазмещение — следующие фазы.

- **2026-06-03 — редизайн главной (Проекты / недавние).** Шапка «Проекты» + разделитель +
  кнопки Новый/Открыть, поиск прижат вправо (`w-64`); контент центрирован (`max-w-[1120px]`).
  Сетка недавних: первой идёт пунктирная плитка «Новый проект» — единый паттерн с галереей
  дизайнов, вынесен в общий `components/ui/DashedAddTile.tsx` (иконка/заголовок/подзаголовок —
  пропсы). Карточки крупнее (`minmax(220px,1fr)`), превью — `components/home/ProjectThumb.tsx`:
  монограмма (первая буква имени) на «pcb-grid» (утилита `.pcb-grid` в `styles.css`) с точкой
  DFM-вердикта в углу. **Монограмма — временный фолбэк**, пока нет композитного превью платы
  (как в `DesignCard`/`LayerStack`). Точка-вердикт — свойство **панели**, не дизайнов внутри;
  пока фиксированно зелёная (`ThumbVerdict` готов к реальным значениям). Подвал карточки: имя +
  «N дизайнов · W×H» (размер — `useUnitFormat`, как в `DesignCard`; только когда панель задана) +
  относительное время. Появление карточек — `.anim-in` (короткий fade/lift, уважает
  `prefers-reduced-motion`). `PcbPreviewPlaceholder` удалён (заменён `ProjectThumb`).
- **2026-06-03 — авторазмещение (nesting) в окне «Добавить дизайн на панель».** Между телом окна и
  футером — **раскрывающаяся полоса «Авторазмещение»** (`components/panel/NestingControls.tsx`):
  кликабельная строка-заголовок (`<div>`, НЕ `<button>` — внутри radix-`Switch`, который сам кнопка;
  Switch обёрнут в `<span onClick=stopPropagation>`, чтобы клик по нему не дублировал тоггл строки),
  иконка `layout-dashboard` (медная когда вкл), хинт, тумблер. Тело (`bg-panel/40`) — 4 группы с
  uppercase-подписями (`text-[10px]`): Заполнение (`Копии|Процент` + степпер/слайдер), Зазоры,
  Укладка (Поворот, **угол-пикер** — 4 точки по углам мини-прямоугольника, активная `bg-primary
  ring-2`, Рядами/Колонками, Шаг), Поведение (Сторона, Перепаковка). Все значения — в
  `settingsStore.nest` (persist), правятся через `setNest`. Правая панель окна получила сегмент
  **Дизайн | На панели**: «На панели» = `PanelLayoutPreview` — мини-бланк (медный inset-контур +
  `.pcb-grid`, fit-to-box по обеим осям) с упакованными копиями простыми div'ами. В футере — строка
  `fitLine` (сетка C×R / «Войдёт N из M» жёлтым при переполнении). Числовые поля — `UnitField`
  с `dim` (конвертируют в активные единицы, см. запись от 2026-06-04).

- **2026-06-03 — фильтр типов DFM-проблем на превью + первое контекстное меню.** На 2D-превью
  инспектора, когда DRC-оверлей включён, рядом с тогглом появляется воронка (`ListFilter`),
  открывающая `Popover` со списком актуальных типов проблем (чекбоксы «показывать на превью»).
  Тот же список — по **правому клику** на холсте (`ContextMenu`; `ContextMenuTrigger disabled`
  пропускает нативное меню, когда фильтр неактивен). Снятая галочка убирает хотспоты типа из
  оверлея и из степпера, не меняя вердикт. Заведены три radix-примитива в `components/ui/`:
  `Popover`, `Checkbox`, `ContextMenu` — это **первое контекстное меню в приложении**, паттерн
  для будущих (правый клик на дизайне/инстансе и т.п.). Гранулярность фильтра — по «типу
  проблемы» (`feasibility.problemTypeOf`), промежуточному между сырым `category` и полным id.

- **2026-06-04 — `UnitField` уважает единицы измерения.** Раньше `UnitField` (поля размера
  бланка/толщины в `PanelEditor`, зазоры/край/шаг в `NestingControls`) показывал и принимал
  сырые мм с литералом «mm» мимо `useUnitFormat` — в имперском режиме не конвертировал (против
  правила «конвертируй на границе UI»). Теперь у поля есть проп `dim` (`"coarse"` мм/дюйм,
  `"fine"` мм/мил): при заданном `dim` поле само гоняет `toDisplay`/`fromDisplay`, подставляет
  суффикс через `unitLabel` и подбирает шаг под активную единицу — модель остаётся в мм
  (`value`/`onChange` всегда мм). Без `dim` — прежнее поведение (сырые мм + литерал `unit`).
  Подписи групп («Зазоры, мм») и хинт `maxFromSettings` («Макс. … мм») лишились зашитой «мм» —
  единицу теперь несёт само значение. Размер/толщина бланка — `coarse`/`fine`, зазоры nesting —
  `fine`.

- **2026-06-04 — схематичное превью «Дизайн» в окне + общий хук `usePreviewData`.** Режим «Дизайн»
  правой панели окна «Добавить дизайн на панель» теперь рисует реальное превью платы через
  `PreviewPane` (2D `LayerStack` линиями цветов слоёв, 3D, Верх/Низ, тумблер «Проверка»/DRC) —
  **без паяльной маски** (схематичный вид: медь/шелк/контур на голой подложке). Машинерия загрузки
  (SVG-слои/сверловка/3D-меш/метрики + деривация находок/маркеров/слоёв) живёт в переиспользуемом
  хуке `hooks/usePreviewData.ts` — движок превью окна (read-only). «Без маски» = опция `excludeMask`:
  отбрасывает `topMask`/`bottomMask` из `layers` (2D) и из `visibleKeys` (3D, клиентский фильтр меша).
  Окно не пишет в проект и не зовёт артефакт-flush (главное окно — единственный писатель). Внизу
  превью — компактная строка имя + `VerdictBadge`. (`DesignInspector` пока держит свою inline-загрузку
  — конвергенция инспектора на `usePreviewData` вынесена в follow-up, см. ROADMAP: после того как
  master вынес инспектор в отдельное окно (#70), оба окна целятся в общий хук отдельным PR.)

- **2026-06-04 — общий источник стилей серьёзности + резолвер текста находок (рефакторинг-дедуп).**
  Карта `severity → {Icon, fg, bg, dot, hsl}` была скопирована дословно в четырёх местах
  (`VerdictBadge`, `FeasibilityTab`, `DrcMarkers`, `PreviewPane`), плюс `SEV_RANK`/`worseSeverity`
  отдельно в `DesignInspector`. Сведены в один `lib/severity.ts` (`SEVERITY`-карта + `SEV_RANK` +
  `worseSeverity`); `hsl` — для SVG/canvas, где класс не повесить, `fg/bg/dot` — tailwind-классы.
  Резолвер `I18nText` находки (длины через `fmtLen`, ключ-подобные параметры через `t`, затем
  ключ текста) жил двумя посимвольными копиями — `FeasibilityTab.useRenderText` и
  `usePreviewData` (куда копия инспектора уехала при конвергенции #81); вынесен в хук
  `hooks/useFindingText.ts` (`tr`/`trLen`/`measuredLimit`, колбэки стабильны через `useCallback`
  — безопасно в deps `useMemo`). Поведение не меняется, минус ~130 строк дублей. Редактор панели
  не затронут.

- **2026-06-04 — общий каркас формовой модалки (рефакторинг-дедуп).** Три диалога имя/описание
  (`RecentSettingsModal`, `ProjectSettingsModal`, `RenameDesignModal`) дословно повторяли футер
  (ghost «Отмена» + «Сохранить», выключенная на пустом имени) и пары label+`TextInput`. Заведены
  `ui/FormModal` (Modal + стандартный футер, `onSave`/`canSave`) и `ui/LabeledTextInput`
  (подпись над инпутом); три модалки переведены на них. Состояние/эффекты/логика сохранения
  остались в каждой модалке (они разные) — выносится только повторяющаяся вёрстка-хром.

- **2026-06-04 — общий `PanelStatus` для loading/empty (рефакторинг-дедуп).** `FeasibilityTab` и
  `MetricsTab` посимвольно повторяли центрированное состояние (спиннер + текст / «нет данных»);
  `Board3D` — почти то же (`size-6`, `w-full`). Заведён `ui/PanelStatus` (опц. `loading`-спиннер +
  опц. `message`, `spinnerClassName`/`className`); три места переведены. `LayerStack` намеренно
  оставлен как есть — его контейнер несёт `ref` для замера размера канваса и показывает спиннер
  без подписи (`text-[12px]`), форсить под общий примитив = риск задеть размер-логику (YAGNI).

- **2026-06-04 — `useDesignVerdict` + `VerdictDot` (рефакторинг-дедуп).** `DesignCard` и
  `DesignPickerRow` дублировали fetch метрик → `overallVerdict(evaluate(...))` с cancel-guard'ом
  (~25 строк × 2); сведено в хук `hooks/useDesignVerdict` (вход: workingDir/gerbers/profile +
  опции panel/stackup/traceSession/onMetrics; выход: `{ verdict, size, settled }`, ключ по набору
  герберов). Точка-вердикт `size-2 rounded-full bg-…` рисовалась руками в трёх местах
  (`DesignCard` — 4 стейта вкл. null→muted, `DesignPickerRow` — своя `DOT`-мапа, `DesignInspector` —
  тинт таба) → `ui/VerdictDot` (на базе `SEVERITY.dot`, null→muted, размер/позиция через `className`).
  Редактор панели не затронут.

- **2026-06-04 — overlay линейки: медь поверх тёмного кожуха (читаемость).** Раньше линия замера и
  точки были одним белым цветом (`rgba(255,255,255,.92)`) — на зелёной маске с белым шёлком и
  светлой медью они сливались (один цвет → где-то всегда низкий контраст). Теперь рисуем в два слоя:
  тёмный кожух `rgba(0,0,0,.6)` снизу + медное ядро `hsl(var(--primary))` сверху. Линия 5px кожух /
  2px медь (round cap); концы — прицел-снап (кольцо r10, перекрестье 32px, центральная точка) тем же
  haloed-стилем; при привязке к фиче доп. пунктирное медное кольцо r15. Катеты ΔX/ΔY — пунктир
  `6 5`, рисуются всегда при активном замере. Тултип (тёмный popover, dist крупно + ΔX·ΔY мелко)
  оставлен, с белым текстом на тёмном фоне. Пока линейка активна — едва заметный скрим платы
  (`rgba(0,0,0,.07)`) для отрыва overlay. Все толщины/радиусы — в экранных px (постоянны при зуме),
  координаты точек — в мм через текущий transform. Код — `LayerStack.tsx` (`M_*`-константы + блок
  `tool === "measure"`).

- **2026-06-04 — общие хуки Tauri-listener'ов + фикс async-cleanup (рефакторинг-дедуп).** Заведён
  `hooks/useTauriListeners` с двумя хуками. `useBridgeListeners(subscribe)` — главная сторона мостов
  (`useInspectorBridge`/`useAddDesignBridge`): регистрирует массив listener'ов на жизнь компонента и
  **синхронно-безопасно** их снимает (active-флаг + накопленные unlisten'ы), чиня StrictMode-утечку
  старого `subs.forEach((p) => p.then((un) => un()))` (unlisten мог зарегистрироваться уже после
  размонтирования). `useSnapshotSubscription(subscribe, emitReady)` — сторона удалённого окна
  (`InspectorWindow`/`AddDesignWindow`): подписка на один snapshot, `ready` шлётся только после того
  как listener жив; возвращает последний снимок. Оба окна и оба моста переведены.

- **2026-06-04 — смарт-направляющие на холсте панели.** При перетаскивании инстанса(ов)
  на холсте панели показываются **magenta-направляющие** (`#ff4d9d`, пунктир `[4,3]`,
  1px на экране — `strokeScaleEnabled=false`), когда AABB выделения выровнено по краю
  или центру другого инстанса либо рамки панели (порог `SNAP_PX = 6` экранных px,
  пересчитывается в мм с учётом текущего зума). Ось с попаданием — магнитный доводящий
  snap; ось без попадания — обычная сетка 1 мм. Alt отключает snap полностью (свободный
  сдвиг). Рамка панели включена как обычная цель — даёт края и центр панели без
  спецкода. Magenta выбрана как контрастная по отношению к синему marquee (`#5b9dff`) —
  другая аффордансность (как в Figma). Реализовано как чистая функция `computeSmartGuides`
  (тестируется в `panelPlacement.test.ts`) + компонент `SnapGuides` (Konva `Line`,
  `listening=false`, не перехватывает pointer).

- **2026-06-04 — динамические линейки + адаптивная сетка на холсте панели (общий CAD-инструмент).**
  Холст панели (`PanelBlankCanvas`, Konva) получил масштабируемый CAD-инструментарий: edge-pinned
  линейки сверху/слева с адаптивными делениями и подписями в активных единицах (мм/inch/mil),
  hover-крестик + плашка-readout «X · Y», стрелки-маркеры курсора, медная подсветка проекции бланка
  на шкалах (линейка «цепляется» к квадрату), origin-маркер 0,0 и пунктирный контур рабочей зоны
  станка (из настроек `maxPanel*Mm`). Фикс-сетка `CadGrid` (5/50мм) на панели заменена адаптивной
  `AdaptiveGrid`, чьи major-линии совпадают с подписями линеек. Архитектура (выбор пользователя):
  tech-agnostic **screen-space SVG-оверлей** `components/editor/RulersOverlay`, управляемый
  дескриптором `{pxPerMm, originX, originY}` (не зависит от движка холста — Konva/SVG), чтобы один
  и тот же рендер линеек можно было подключить и к превью дизайна (схождение — следующей фазой).
  Чистая тик-математика вынесена в `lib/canvasTicks` (`STEP_LADDER`/`pickStep`/`gridSteps`/`ticksFor`,
  юнит-тесты) — её делят оверлей и сетка, поэтому деление сетки и подпись линейки всегда совпадают.
  Координаты — в мм через `viewport`, толщины/радиусы — в экранных px (постоянны при зуме);
  hover-апдейты коалесцируются в один на кадр (rAF), чтобы движение мыши не перерисовывало дерево
  инстансов. Источник прототипа — дизайн-хэндофф (Canvas2D-референс), воссоздан на Konva/SVG.

- **2026-06-04 — превью дизайна на общий `RulersOverlay` + отключаемый крестик (Фаза 2).** Превью
  (`LayerStack`, SVG) переведено с собственных in-scene линеек на тот же screen-space `RulersOverlay`,
  что и панель — единый рендер линеек на двух разных движках (Konva/SVG). Inline-тик-математика
  (`STEP_LADDER`/`pickStep`/`ticksFor`/`fmtMm`) удалена, превью-сетка теперь на `lib/canvasTicks` →
  деления сетки и подписи линеек гарантированно совпадают. Подписи превью переведены на активные
  единицы (`fmtLen`, было всегда мм). Привнесён «богатый» набор как на панели: медная подсветка платы
  на шкалах + origin-маркер. Линейки превью используют немирроренный `tx + s·mm` (как и сетка), якорь —
  край платы (`edgeBox.min`); за счёт пивота `mid/midx` якорные тики ложатся на визуальный габарит
  платы с обеих сторон, поэтому единой возрастающей оси хватает (sign/flip не нужен). Толщина полос
  линеек — пропсы `rulerTop/rulerLeft`: превью оставлено на слим-20px (без правок fit/rPad), панель —
  на дефолтных 24/30. readout сделан относительным к якорю (совпадает с подписями). **Hover-крестик —
  отключаемый** (кнопка `LocateFixed` на зум-баре обоих холстов, **по умолчанию OFF**; на превью ещё и
  скрыт в режиме замера). Курсор отслеживается только при включённом крестике.

- **2026-06-04 — акцент линеек: медь только на панели, превью → нейтральный тёмный.** На превью дизайна
  медная подсветка платы на шкалах (+ риски + origin) спорила с цветами платы (зелёная маска/медь/шёлк) —
  читалось хуже, чем прежний тёмный вариант. Введён проп `RulersOverlay.extentVariant`: `"copper"` (дефолт,
  панель) и `"muted"` (превью) — подсветка `hsl(var(--muted-foreground)/0.12)`, риски/origin
  `hsl(var(--muted-foreground))`. Hover-крестик/readout остаются медными (он opt-in). Панель не тронута.

- **2026-06-04 — линейки панели тоже на `extentVariant="muted"`.** На холсте панели бланк медный и
  заполняет почти весь вид, поэтому медная подсветка проекции на шкалах закрашивала линейку целиком
  (выглядело как сплошная медная полоса). Панель переведена на тот же нейтральный вариант, что и превью
  дизайна (`extentVariant="muted"`) — линейка читается ровно, как в инспекторе. Проп `"copper"` остаётся
  в API (дефолт), но в продукте сейчас не используется. Hover-крестик/readout по-прежнему медные.

- **2026-06-04 — общий `ConfirmDialog` (первый confirm-диалог).** Заведён переиспользуемый
  `components/ui/ConfirmDialog` поверх существующего `Modal`: заголовок + текст + пара кнопок
  (отмена-`ghost` / подтвердить-`destructive` по умолчанию, `destructive={false}` для неразрушающих).
  Подтверждение зовёт `onConfirm` и закрывается; Escape/бэкдроп/отмена — просто закрывают. Первый
  потребитель — удаление размещённого дизайна (карточка галереи): если дизайн стоит на панели, удаление
  спрашивает подтверждение (каскад снимет и дизайн, и его `BoardInstance`), неразмещённый удаляется
  сразу. Паттерн для будущих деструктивных действий (раньше в приложении confirm-диалога не было).

- **2026-06-04 — числовой инспектор размещения в правом доке.** В `PanelInspector` при выделении
  инстанса(ов) сверху скролл-тела появляется секция «Размещение» (`PlacementFields`): X / Y / Поворот
  с точным вводом + габариты read-only. X/Y — верхний-левый угол повёрнутого AABB в мм от начала
  панели (совпадает с линейками/ридаутом крестика), правки клампят плату обратно в панель. Поля длины —
  общий `UnitField` (активные единицы); для угла заведён компактный `DegreeField` (TextInput + суффикс
  «°», draft-while-editing как в `UnitField`). Секция самодостаточна (подписана на selection/panel
  стораны), поэтому презентационный `PanelInspector` почти не оброс пропсами. Мульти-выделение — пока
  счётчик-подсказка (точные координаты для одной платы).

- **2026-06-04 — красная подсветка off-panel инстансов.** Дизайн, торчащий за край панели,
  на холсте рисуется красным контуром + слабой красной заливкой (`INSTANCE_OFF_STROKE` `#e5484d`,
  `INSTANCE_OFF_FILL` `rgba(229,72,77,0.12)` в `canvasStyle`); подпись остаётся нейтральной для
  читаемости, контур чуть толще (1→1.5 px). Красный = «почини раскладку», согласован со счётчиком
  предупреждения «N выходят за край». Красный — единственный «алертный» акцент на холсте; медь
  по-прежнему только для действия/выделения, нейтраль — для структуры/контента.

- **2026-06-04 — янтарная подсветка warn-инстансов + панельная сводка в доке.** Введён токен
  `INSTANCE_WARN_STROKE` (`#f59e0b`) в `canvasStyle` — янтарный контур (без заливки) для инстансов
  с некритичными нарушениями (слишком близко к краю или к другой плате, выход за рабочую зону).
  Логика: block=красный контур+заливка, warn=янтарный контур, ok=нейтраль. Источник — хук
  `usePanelFindings` (lib/panelFeasibility.ts: `evaluatePanel`); холст и счётчик привязаны к нему.
  В доке «Параметры панели» (`PanelInspector`) добавлена секция «Проверка раскладки»: бейдж-вердикт
  (пилюля цветная по серьёзности) + список находок с иконками серьёзности; клик по строке выделяет
  виновные инстансы на холсте.

- **2026-06-04 — свойства выделенной платы → плавающий HUD у объекта.** Свойства выделённого
  экземпляра (имя, X/Y, «повернуть 90°», размер, дублировать/удалить) вынесены из правого
  инспектора (#134) во всплывающий `SelectionHud` — экранный HTML-оверлей над платой (сосед
  `RulersOverlay`, вне Konva-Stage). Позиция — из живого bbox через `viewport` (`originX + mm·pxPerMm`),
  чистая `lib/hudPlacement` (top→flip-bottom, clamp по X, каретка над центром); экранно постоянный
  размер, compact-режим (только действия) на узкой плате. Правый инспектор снова чисто панельный
  (секция «Размещение» и `PlacementFields` удалены). HUD — только для одной платы; мульти оставляет
  `PanelAlignBar`. **Маркеры выделения** (`SelectionOverlay`): медное кольцо + 4 угловых квадратных
  хэндла (медь, обводка цветом фона) вместо прежнего map-pin (pin занимал зону HUD). **Свободный
  поворот** (`RotationHandle`, кноб-на-стике) переехал с верх-центра на угол bbox (диагональный
  вынос) — top-центр отдан HUD; пивот вращения остался центром выделения.

- **2026-06-04 — запретные зоны (keep-out) на холсте панели.** Введён инструмент
  «Запретная зона» в `PanelToolPalette` (иконка lucide `OctagonAlert`). Зоны рисуются
  drag-ом на пустом холсте, выделяются кликом и перемещаются drag-ом. Выделение зон —
  **отдельный** `keepOutSelectionStore` (не мешается с выбором плат: клик по плате чистит
  зоны, клик по зоне чистит платы).

  Цветовой токен (в `canvasStyle.ts`, приглушённый нюанс — НЕ красный, который зарезервирован
  для off-panel):

  | Заливка | Контур |
  |---|---|
  | `rgba(99,130,190,0.13)` | `rgba(99,130,190,0.7)` (серо-синий) |

  Выделенная зона — медный контур (`COPPER_STROKE`) снаружи. Рендер (диагональная
  штриховка + заливка) в `KeepOutLayer.tsx`. Тип зоны (`kind`) удалён в рамках #248 —
  зона единая, запрещает одновременно платы и тех. отверстия.

- **2026-06-04 — производные зоны зажима реперов на холсте панели.** Введён `ClampZoneLayer`
  (`cuprum-ui/src/components/panel/ClampZoneLayer.tsx`) — отрисовывает по одному пунктирному
  прямоугольнику на каждое реперное/флип-отверстие с ненулевым радиусом зажима. Размер квадрата =
  диаметр отверстия + 2·radius (вычисляется через `clampZonesForHoles` из `panelPlacement`).
  Зоны **не-интерактивны** (`listening={false}`), следуют за отверстием автоматически и исчезают
  при radius=0. Цвет — приглушённая охра (`CLAMP_FILL`/`CLAMP_STROKE`), отличается от:
  ручных keep-out зон (единый серо-синий), красного off-panel и янтарного warn.
  В «Настройках → Возможности → Размер панели» добавлено поле «Поле зажима реперов»
  (`toolingClampRadiusMm`, `dim="fine"`). Слой рендерится под платами (рядом с `KeepOutLayer`).

- **2026-06-04 — каретки положения на линейках при наведении.** `RulersOverlay` показывает позицию
  курсора на каждой шкале: лёгкая **каретка** (маркер-треугольник) скользит по линейке (X на верхней,
  Y на левой), чтобы свериться с тиками, не включая крестик. **Без подписей** — каретки одни не
  перегружают холст; точное число доступно через крестик/линейку. Каретка — нейтральным акцентом.
  Всегда-он при ховере над плотом; полных линий через холст НЕ рисует. Оверлей сам слушает курсор
  (свой `pointermove` + rAF) — хост не перерендеривается на голом ховере. (Эволюция: тёмные
  плашки-пузырьки → каретка + текст с гало → только каретки, как наименее шумный вариант.)

- **2026-06-05 — панель отвязана от рабочей зоны станка (редактор панели).** Из редактора
  панели убраны: пунктирный контур рабочей зоны (`maxPanelWidthMm`/`maxPanelHeightMm`) в
  `RulersOverlay`, кламп размера бланка по настройкам профиля и amber-алерт «Больше рабочей
  зоны» в `PanelEditor`. В `CanvasToolbar` снят бейдж «Рабочая зона». Цветовая роль «рабочая
  зона» (`work-area`) исключена из DFM-вердикта панели. Причина: панель — самостоятельная
  сущность, задающая физический размер заготовки FR4; ограничение «влезает ли раскладка в
  конкретный станок» — операционное и относится к `ProcessStep`, а не к редактору панели.
  Поля `maxPanel*Mm` в `CapabilityProfile` и страница Настроек сохранены (используются
  машинным профилем). Операционная проверка вынесена в follow-up (#246, эпик #178).

- **2026-06-05 — живой маркер позиции станка на холсте сверловки.** В окне сверловки
  (`DrillWindow`) на Konva-холсте (`DrillMapCanvas`) рисуется `MachineMarker` — экранное
  перекрестие + центральная точка. Маркер существует в **экранном пространстве** (постоянный
  размер, не масштабируется вместе с панелью). Цвет — циан `#22d3ee` (контрастен к медному
  акценту и зелёным «просверленным» отверстиям). Рядом с маркером — числовой ридаут рабочих
  координат X / Y в мм (рабочая СК станка: ноль = левый нижний угол панели, Y↑ вверх).
  Переход между отсчётами поллера (5 Гц) сглаживается линейным твином 0.18 с (`Konva.Tween`)
  — резкие скачки не видны. Маркер виден только во время активного прогона
  (`running` / `paused` / `awaitingToolChange`) **и** пока поступают свежие данные (`machine://status`
  последний раз пришёл < 1 с назад); при `machineWork == null` холст рендерится без изменений.
  Координатное преобразование рабочая СК → экран: `workPosToPanel` (Y-флип) → scale `fit` + offset
  из fit-группы.

- **2026-06-07 — экран сверловки v2: привязка нуля как режим инспектора.** Правый инспектор
  сверловки (`DrillPlanInspector`) переключается между под-режимами `plan` ⇄ `zero` (внутренний
  стейт `panelMode`), **холст при этом не меняется** — свапается только сайдбар. Прогон
  (`run.state.phase`) по-прежнему перехватывает инспектор поверх под-режима. В плане вместо
  громоздкой карточки нуля — компактная **карточка-кнопка `WorkZeroStatusCard`** (иконка-статус +
  «не выставлен» / «выставлен · угол» / amber «смещён — выход за ход» + «Настроить ›»). Сам
  подвод/Z/привязка живут в `DrillZeroInspector` (шапка «‹ План», `DatumCornerPicker`,
  мини-карта, тело `WorkZeroCard`). Паттерн «режим-свитч сайдбара без смены холста» — для
  сфокусированных под-задач, не уводящих оператора с рабочего контекста.
- **2026-06-07 — фильтр видимости = выбор (тулбар холста сверловки).** Из `DrillCanvasTopBar`
  убраны чипы видимости классов («Показать»): на холсте подсвечено то, что выбрано в плане
  справа (выбранное — alpha 1, остальное — бледный контекст alpha ~0.25), дублирующего фильтра
  нет. В тулбаре остались Путь / Диаметры + подпись-подсказка. Принцип: один источник истины для
  «что показываем» — выбор в плане, без параллельного независимого фильтра.
- **2026-06-07 — мини-карта «плата на столе» (`DrillTableMap`).** В режиме привязки нуля —
  inline-SVG поверх хода станка (viewBox в машинных мм, Y-флип): пунктирный прямоугольник хода,
  прямоугольник платы (медь) на живой позиции шпинделя (= будущий ноль), красные overflow-полосы
  за ходом, точки выбранных отверстий по категориям, медный маркер датума. Под картой — статус
  ✓ «помещается» / amber «выход за ход X+.. Y+..». **Клик — подвести ноль сюда**: точка
  инвертируется в машинные координаты, клампится в ход, переводится machine→work через WCO и
  отрабатывается абсолютным `jogTo` (тот же паттерн клик-перемещения, что у `WorkField`/Z-шкалы
  `WorkZeroCard`). Чистая модель (`lib/tableMap.ts`: `panelOnTable`/`envelopeFit`/`holeTablePoints`)
  — в мм, покрыта тестами; проекция через общий `machinePoint`, что и прогон/XY-гейт.

- **Шапка инспектора дизайна = зеркало шапки проекта.** Убрали инлайн-редактируемое имя из
  шапки окна инспектора (имя и так в заголовке ОС-окна); переименование переехало под
  шестерёнку справа (`DesignSettingsModal` — `FormModal` с полем имени поверх emit-колбэка,
  т.к. инспектор — удалённое окно и не ходит в `useShell`, в отличие от галерейного
  `RenameDesignModal`). Табы Превью/Характеристики/Проверка переведены со склеенного
  `SegmentedControl` на «бамбу-стиль» `NavTabs` (с иконками Eye/Ruler/ShieldCheck, спиннером
  при загрузке метрик и точкой-вердиктом на «Проверке»). `NavTabs` вынесен как общий компонент
  и теперь питает и `ProjectPage`, и инспектор — раскладка шапок совпадает (табы слева,
  шестерёнка справа).

*Ревизия 2026-06-07. Обновлять при изменении дизайн-системы и принятии новых
дизайн-решений (добавляй запись в лог выше).*
