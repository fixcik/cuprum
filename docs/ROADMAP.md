# Cuprum Roadmap

Дорожная карта ближайших шагов. Текущий статус и план до первой реальной
платы. Видение и обоснование выбора — [`VISION.md`](VISION.md); модель
данных — [`PROJECT.md`](PROJECT.md); канонические термины —
[`GLOSSARY.md`](GLOSSARY.md).

## Где мы сейчас

**Готово:**

- Парс Gerber, растеризация (tiny-skia), композ на экран 15120×6230
- `.goo` энкодер, SDCP клиент (discovery / upload / expose)
- UV-засветка работает в железе (Elegoo Saturn 4 Ultra 16K)
- Tauri UI с превью, multiselect + marquee, align/distribute, persist
- CLI: `discover`, `gerber-info`, `render`, `prepare`, `upload`, `expose`
- Стартовый экран + каталог недавних (SQLite MRU), контейнер `.cuprum` (ZIP)
- Документ проекта: working-dir (распаковка в temp, loose-чтение, recovery/GC),
  автосейв, undo/redo, restore points; `panel` свёрнут в манифест (схема v4)

**Чего нет:**

- Panel-модели в UI: структуры данных (`BoardInstance`/`ToolingHole`, `PanelDoc`
  v2) заведены, но **редактора размещения и Panel-превью ещё нет** (дизайны пока
  не кладутся на панель)
- CNC вообще: ни G-code, ни ToolLibrary, ни auto-leveling
- Ни одной реальной платы в руках

## Workstream: модель проекта `.cuprum` + UX дизайнов

Инфраструктура документа проекта и UX управления дизайнами — фундамент под
Panel-модель ниже. Ведётся инкрементальными PR по принятому рабочему процессу.

- [x] **Фаза 1 — working-dir** (✅ 2026-05-31, PR #5): открытие распаковывает `.cuprum` в temp,
      чтение/рендер по loose-файлам, recovery + GC осиротевших папок.
- [x] **(рефактор) panel → manifest** (✅ 2026-05-31, PR #6): `panel.json` свёрнут в манифест
      (схема v4 + миграция легаси) — документ стал одним файлом.
- [x] **Фаза 2 — автосейв + undo/redo + restore points** (✅ 2026-06-01, PR #7): снимки манифеста
      для undo/redo, контрольные точки в `history/` (ручная кнопка 💾 + авто на
      открытии с дедупом), тулбар ◀/▶/💾 + хоткеи; импорт отменяем.
- [x] **Фаза 3 — схлопывание импорта** (✅ 2026-06-01, [PR #8](https://github.com/fixcik/cuprum/pull/8)):
      `addDesignFromZip` (копирование в working-dir + авто-классификация),
      удаление staging-команд и визарда; относительное время в restore points.
- [x] **Фаза 4 — галерея дизайнов + инспектор** (✅ 2026-06-01, [PR #8](https://github.com/fixcik/cuprum/pull/8)): карточки по
      дизайну (превью-thumbnail + DFM-вердикт), drag'n'drop ZIP в каталог,
      удаление дизайна, переиспользуемый инспектор (2D/3D + редактируемые типы
      слоёв + DFM через `projectBoardMetrics`); удаление `ImportWizardPage`/`DesignsTab`.
      DFM-проверка размера дизайна — против панели проекта (фолбэк на макс. станка),
      размер панели клампится по рабочей зоне из настроек.
- [x] **Фаза 5 — открытие по клику** (✅ 2026-06-01, [PR #9](https://github.com/fixcik/cuprum/pull/9)):
      ассоциация `.cu`/`.cuprum` (дефолт `.cu`); `RunEvent::Opened` + single-instance + argv →
      `take_pending_open`/событие `open-file` в `App.tsx`; идемпотентный `openProjectByPath`;
      dock-recents на macOS (`NSDocumentController.noteNewRecentDocumentURL`).

Бэклог:
- [x] **Рендер-артефакты как часть проекта** (✅ 2026-06-02): SVG-слои, DFM-метрики и
  цветное превью дизайна стали персистентными content-keyed артефактами в
  `<workdir>/artifacts/{svg,metrics,preview}` — едут внутри `.cuprum` (упаковка
  `workdir::pack`), переданный проект не пересчитывает рендер/метрики (~1с+~6.6с на
  дизайн). Версии артефактов сведены в `cuprum_core::artifact`; устаревшее вычищается
  `artifact::gc` при упаковке. Карточка дизайна рисует одно растровое превью (`<img>`,
  backend-композит через `resvg`) вместо тяжёлого SVG-DOM — фикс торможения фронта на
  большой сетке.
- [x] **Умный ретеншен точек восстановления + GC осиротевших гербер** (✅ 2026-06-02):
  точки несут флаг `auto`; ручные (💾) пиннятся, авто-точки прореживаются по времени
  (GFS: всё <24ч → 1/день·7д → 1/неделю·8нед), устаревшие авто (ссылаются только на
  удалённые дизайны) выбрасываются после 48ч. При `pack` `gc_gerbers` освобождает
  `gerbers/<id>/`, на которые не ссылается ни манифест, ни одна оставшаяся точка —
  переданный проект больше не таскает байты давно удалённых дизайнов. Чистая логика
  (`select_pruned`) тестируема; немигрируемые снимки сохраняются (fail toward
  retention).
- [x] **Ускорить flush (перепаковку `.cuprum`)** (✅ 2026-06-03, [PR #43](https://github.com/fixcik/cuprum/pull/43)):
  `container::write` больше не дефлейтит всё уровнем 6 на каждый репак. Три рычага: per-entry
  политика сжатия (PNG-превью → `Stored`, текст → `Deflated` level 1); инкрементальный репак
  (неизменные записи переносятся `raw_copy_file` по совпадению имени и crc32, без передефляции;
  фолбэк на полный репак при отсутствии/битости предыдущего архива); коалесцинг частоты флушей
  на фронте (серия репаков, пока один в полёте, схлопывается в ≤2 трейлинг-репака). По трассе
  импорта `zip_write` упал с ~1.1 с до ~25–55 мс (×20–50, при бóльшем числе записей), полный
  `flush` ~1.2 с → ~50–100 мс. Формат остаётся ZIP-совместимым, логический вывод не меняется,
  кеш-теги не тронуты.
- [x] **Редизайн главной (Проекты / недавние)** (✅ 2026-06-03): плитка «Новый проект»
  первым элементом сетки (общий `DashedAddTile`, вынесен из галереи дизайнов), крупные
  центрированные карточки (`minmax(220px)`, `max-w-[1120px]`), монограмма-превью на «pcb-grid»
  как временный фолбэк до композита платы, контекст на карточке «N дизайнов · W×H». Каталог
  недавних расширен полями `design_count` + `width_mm/height_mm` (персист при open/save, refresh
  на autosave без сдвига `last_opened_at`). Точка DFM-вердикта на карточке пока фиксированно
  зелёная — вердикт это свойство **панели**, посчитаем отдельно (см. ниже).
- [x] **Редизайн вкладки «Дизайны»** (✅ 2026-06-04, [PR #102](https://github.com/fixcik/cuprum/pull/102)): хедер вкладки
  (заголовок + чип-счётчик + сводка DFM «N можно изготовить · N с предупреждением · N нельзя
  изготовить» + поиск + «Добавить»); богатые карточки (стеклянный чип-вердикт, превью на `pcb-grid`
  с защитным градиентом и contain/cover по габаритам, hover-ряд «На панель»/«Открыть», бейдж
  «На панели · N» из размещений, углы переименовать/удалить); add-тайл первым в сетке; пустое
  состояние-дропзона; скелетон импорта с `ProgressRing` (счётчик `importingCount`); drag-overlay на
  всю вкладку. «На панель» открывает окно «Добавить дизайн на панель» с предвыбранным дизайном
  (поле `preselectDesignId` в снапшоте моста; доставка и для уже открытого окна). Сводка вердиктов —
  чистый `rollupVerdicts`, карточки репортят вердикт наверх.
- [x] **«На панель» раскладывает в свободные ячейки** (✅ 2026-06-04, [PR #108](https://github.com/fixcik/cuprum/pull/108)):
  чистая `packLayoutAvoiding` генерит сетку от угла, но пропускает ячейки, перекрывающие уже
  размещённые инстансы (их AABB надувается на зазор `nest.gapMm`), и кладёт первые `requested`
  свободных; существующие платы не двигаются. `packLayout` стал тонкой обёрткой (пустые препятствия
  → прежнее поведение). `addBoardInstances` собирает размеры размещённых дизайнов (кеш-метрики) и
  строит препятствия. Превью «На панели» в окне честное: рисует существующие инстансы приглушённо и
  кладёт новые копии в свободные ячейки; `fitLine` различает «больше панели» / «нет места» / overflow.
- [ ] **Панельный DFM-вердикт для карточки недавних (про ПАНЕЛЬ, не про дизайны).** Точка-вердикт
  на карточке — свойство **панели** (заготовка + размещённые на ней `BoardInstance` и их
  совокупная feasibility), а НЕ отдельных дизайнов. **Зависит от готового размещения дизайнов на
  панели** (Phase 1 — Panel-модель: редактор размещения + nesting): пока класть дизайны на панель
  по сути нельзя, агрегировать нечего, поэтому точка фиксированно зелёная. Когда размещение
  заработает — свернуть feasibility панели в `ok/warn/block`, персистить в каталоге, подставить
  вместо зелёного. Дизайн самого вердикта/агрегации ещё **не брейнштормили** — вернуться, когда
  размещение будет готово.
- **Удаление дизайна ↔ панель.** Удаление уже снимает ссылку из манифеста, а байты
  осиротевших дизайнов теперь вычищаются GC при упаковке (см. выше). Остаётся: когда
  появится размещение дизайнов на панели (`BoardInstance`), удаление дизайна должно
  снимать и его размещения с панели (и наоборот — нельзя удалить дизайн, «молча»
  оставив висящий placement).
- [x] **Тесты для фронта (тест-раннер + покрытие чистой логики).** Заведён vitest
  + jsdom (конфиг — блок `test` в `vite.config.ts`, алиас `@/`), `pnpm test` в
  `package.json`; шаг `pnpm test` в CI (ui-job в `ci.yml`) + локальный хук
  `cuprum-guard` гоняет его перед `gh pr create`, если ветка трогала фронт. На
  `master` ~144 теста (16 файлов), покрыта вся чистая логика фронта:
  - `feasibility.ts` — DFM-вердикты: size/layers/thin-trace/overallVerdict +
    метрико-зависимые находки (зазор/ширина/поясок/сверловка/via/маска/шелк/overshoot,
    инвариант «info не эскалирует вердикт») (✅ 2026-06-03, PR #60, #67).
  - `layerColors`/`panel` (PR #71), `panelPlacement`/`nest` — grid-паковка, bbox с
    поворотом, off-panel (PR #74), `artifactProgress`/`projectErrors` (PR #75),
    `relativeTime`/`formatRelativeTime`/`resolveLanguage` (PR #78),
    `boardMesh` (бинарный wire-парсинг) / `boardOutline` stitchLoops (PR #85) (✅ 2026-06-04).
  - Единицы: useUnitFormat **отрефакторен** в чистое ядро `unitFormat.ts` (мм↔милы/дюймы)
    + тесты (PR #82); стораы: `settingsStore` (PR #86), `store` (select/align/distribute/
    autoArrange) (PR #91), `shellStore` sync-surface (view/прогресс артефактов/undo) (PR #93) (✅ 2026-06-04).
  - **Дальше (по желанию):** async-флоу `shellStore` (open/save/import — нужна мок-инфра
    `api`-слоя) и `outlineLoops`/`outlinePathD` (SVGLoader) — оставлены под отдельный
    интеграционный заход.
- **Автообновление приложения.** Tauri 2 updater plugin: проверка новой версии,
  скачивание и применение бандла. Стыкуется с `getVersion()` (версия в UI уже оттуда).
  - [x] **PR1 — ядро** (✅ 2026-06-03, PR #55): плагины updater+process, endpoint на
    `latest.json` (GitHub Releases) + minisign-подпись (`createUpdaterArtifacts`,
    signing-секреты в `release.yml`), тихая автопроверка при старте + ненавязчивый
    баннер с прогрессом → relaunch; README про обход Gatekeeper на macOS.
  - [x] **PR2 — ручная проверка в нативном меню** (✅ 2026-06-03, PR #57): нативное
    меню (Tauri 2 Menu API) с «Проверить обновления…» → громкая проверка + тост
    «У вас последняя версия»/ошибка; Edit/Window для системных хоткеев; ярлыки ru/en
    по системной локали (`sys-locale`). Семя пункта «кастомное меню приложения».
  - [x] Секрет репо `TAURI_SIGNING_PRIVATE_KEY` добавлен (✅ 2026-06-03); пароль ключа
    пустой — прописан литералом в `release.yml`, отдельный секрет не нужен (GitHub
    запрещает пустые секреты).
  - Apple Developer ID подпись/нотаризация macOS — отдельной задачей (см. ниже про подпись).
- [x] **DFM: двусторонний дизайн на односторонней панели** (✅ 2026-06-03, PR #51).
  Находка `layers.doubleSided` (severity `block`) в `feasibility.ts`: блок при
  `copperTop && copperBottom && !double_sided`. Нет stackup → считаем двусторонней
  (`?? true`). Дефолт новых панелей флипнут на двусторонний (`DEFAULT_STACKUP` +
  пресеты). Дальше — блок размещения `BoardInstance` в редакторе панели (с редактором).
- [x] **Фикс: правка размера панели стирала размещённые дизайны** (✅ 2026-06-03, PR #62).
  Автосейв редактора звал `savePanelConfig(newPanelDoc(...))` — пустой документ, обнулявший
  `instances`/`tooling_holes` при любой правке размера/стэкапа. Теперь следующий документ
  строится из текущего (`{ ...prev, width_mm, height_mm }`), раскладка сохраняется. При
  уменьшении заготовки, оставляющем дизайн за краем, — ненавязчивое предупреждение под полями
  размера (`isOffPanel`, с учётом поворота).
- [x] **Инспектор дизайна в отдельном OS-окне (мультиокно)** (✅ 2026-06-04, PR #70).
  Клик по карточке открывает инспектор в нативном окне (label `inspector:<id>`), можно
  держать несколько дизайнов разом; встроенный инспектор убран. Подход как у окна
  add-design: главное окно — источник истины (`shellStore`), реактивно броадкастит снимок
  (`{ workingDir, currentPath, manifest }`), окна шлют интенты (rename / set-layer-type /
  artifacts-fresh); тяжёлый рендер считается локально (общий Rust-процесс). `DesignInspector`
  отрефакторен в компонент без `shellStore` (данные/колбэки пропсами). Окна сами закрываются
  при удалении дизайна / смене-закрытии проекта; Rust закрывает `inspector:*` при закрытии
  главного окна. Новый мост `useInspectorBridge`.
  - [x] **Синхронная очистка слушателей в `useInspectorBridge`/`useAddDesignBridge`** (✅ 2026-06-04).
    Старый cleanup делал `unlisten` через `.then()` (async) — в dev-StrictMode при двойном mount
    мог зарегистрировать unlisten уже после размонтирования (кратковременно дублирующиеся слушатели:
    в проде безвредно, но лишний `scheduleArtifactFlush` = лишняя перепаковка). Оба моста разом
    переведены на общий `hooks/useTauriListeners` → `useBridgeListeners` с синхронно-безопасным
    cleanup (active-флаг + накопленные unlisten'ы; ещё в полёте — снимается сразу при резолве).
    Заодно окна `InspectorWindow`/`AddDesignWindow` сведены на `useSnapshotSubscription` (тот же файл).
- **Кастомное меню приложения (menu bar).** Своё нативное меню в строке меню
  (macOS) / меню окна (Win/Linux) вместо дефолтного Tauri — **развиваем
  постепенно**. Пункты связываем с действиями `shellStore`: File (Новый проект,
  Открыть, Открыть недавние, Сохранить-точку), Edit (Undo/Redo), View и т.д.
  Tauri 2 `Menu`/`MenuItem` API. Стыкуется с open-by-click и dock-recents
  (Фаза 5) — единая «оболочка» приложения вокруг проекта.
- [x] **Кросс-операционный парс-кеш герберов** (✅ 2026-06-03). На холодном первом показе
  три независимые операции (`board_mesh` / `board_metrics` / SVG-batch) парсили одни и те же
  гербер-байты — конкурентно, т.е. большой медный слой разбирался ~3× одновременно (лишний
  CPU + контеншн за ядра). Добавлен общий `cache::parse_layer_cached`: memory-only кеш
  `Arc<GerberLayer>` по `hash(bytes)` с per-key single-flight — слой парсится один раз на все
  три операции (конкурентные промахи дедупятся, проигравшие ждут и берут готовый `Arc`). Все
  холодные парс-сайты (`svg::render_layer_svg`, `geometry::{layer,region,mask}_polygons`,
  `mesh::edge_segments`, metrics `parse_all`) переведены на него; metrics держит
  `Arc<GerberLayer>` вместо owned. Дисковый тир не нужен (`GerberLayer` не `Serialize`, а
  выходы каждой операции и так персистятся); вывод не меняется → бампы кеш-тегов не нужны.
- [x] **Не рендерить SVG-слой по нескольку раз** (✅ 2026-06-03). Композитное превью
  дизайна строилось из тех же per-layer SVG, что и инспектор, но `render_design_preview`
  рендерил слои **заново**, мимо общего SVG-кеша (прямой `svg::render_layer_svg` вместо
  кешированного `cache::layer_svg_artifact`) — на холодном показе карточки каждый слой
  рендерился дважды. Теперь превью идёт через `cache::layer_svg_artifact` (тот же
  content-key, что у инспектора, каталог `<artifacts>/svg`): слой рендерится один раз на
  оба, single-flight дедупит даже при параллельном показе. PNG побайтово тот же (внутренний
  id `<mask>` в растре невидим), `preview_key` content-based → бамп `PREVIEW_VERSION` не
  нужен. Смежно с парс-кешем (там — parse-once, тут — render-once).
- [x] **Шелк «бусинами» на сильном зуме** (✅ 2026-06-03). Оказалось — не артефакт
  данных, а баг рендера: `clip-path` по контуру платы, навешенный в #41 на
  силк/медь-группы, под зум-трансформом `scale()` заставлял WKWebView (движок Tauri)
  растеризовать группу в офскрин-буфер в до-масштабной системе координат — тонкие
  штрихи схлопывались в доли пикселя, и зум множил их в «бусины». Воспроизводилось
  только в WKWebView (в Chrome/resvg — гладко). Фикс: силк/медь снова рисуются без
  клипа (как до #41); клип на подложке и паяльной маске — сплошных заливках, на
  которых бага нет, — оставлен. Превью карточки клипает свой композит отдельно
  (`preview.rs`).
- [x] **Склейка штрихов в полилайны** (✅ 2026-06-03, PR #47/#48/#49). Общий
  предрасчёт `strokes::coalesce_strokes` сшивает связанные `Line`-сегменты одной
  ширины/полярности в полилайн. Геометрия обводит полилайн один раз (offset-прямоугольники
  + по окружности на вершину вместо 2 на сегмент) → легче юнион для зазоров/меша
  (`metrics-v16`, `mesh-v6`); SVG рисует полилайн одним `<path>` (`svg-v2`) → меньше
  объём/DOM и глаже зум. Дальше — при необходимости спатиальный индекс для O(n²)-зазоров
  (отдельно, по замеру).
- **Ускорить самый длинный слой 3D-меша.** После оптимизации сборки меша (PR #38:
  триангуляция грани подложки один раз + `rayon::join` подложки со слоями) потолок
  wall-clock плотной платы ≈ самый длинный поверхностный слой (на `plaid` — Silk
  ~400 мс, медь ~360 мс). Слои уже триангулируются параллельно (`board_geometry`),
  так что общий результат держит **самый дорогой** слой: булевы операции
  `geometry::layer_polygons` / `fill_polygons` (union заливок + разность сверловки).
  Идея: ускорять стоимость одного тяжёлого слоя (профиль фаз внутри `layer_polygons`,
  возможно дешевле union/диффа или меньше тесселяции дуг), а не распараллеливание —
  оно уже есть. Профилировать через mesh-трейсинг (`CUPRUM_TRACE=1`, PR #37).
- ~~**Ускорить сериализацию меша (`pack_board_mesh`).**~~ **Исследовано — не делаем**
  (✅ 2026-06-04). Бенч (criterion A/B на плотном `plaid`, блоб 15.66 МБ) показал: в
  **release** упаковка ≈ **5 мс**, а не 110–180 мс — те цифры были debug-трейсами под
  нагрузкой (`tauri dev`), они «не решают перф-вопрос» (как и предупреждает комментарий в
  `board_metrics`-бенче). Bulk-вариант (`bytemuck::cast_slice` срезом вместо поэлементного
  `to_le_bytes`) оказался **на ~5–10% медленнее**: LLVM и так компилит поэлементный цикл в
  эффективный код, обе версии упираются в пропускную способность памяти на ~15 МБ. Узким
  местом упаковка не является; оптимизация отброшена.
- [x] **Удешевить prereqs флуша (`compute_valid_keys`)** (✅ 2026-06-04). После оптимизации репака
  (PR #43) `zip_write` перестал доминировать — в `flush` лидировал `compute_valid_keys` (~29 мс на
  плотной плате): `valid_artifact_keys` в `workdir::pack` **перечитывала с диска и хешировала байты
  всех герберов**, а те же байты тут же снова читал `collect_entries` (~13 мс). Два рычага: (1) сам
  cache-хешер (`diskcache::Hasher`) переведён с **MD5 на xxh3-128** — некриптостойкий, но это
  content-ключи артефактов, не граница безопасности; 128 бит держат ту же ширину ключа (32 hex), а
  скорость ~10× (MD5 ~500 МБ/с был корнем стоимости). Ускоряет **все** content-ключи (svg/metrics/
  preview/parse-cache), не только флуш. MD5 остаётся в SDCP-аплоаде (заголовок `S-File-MD5` требует
  именно его). (2) `pack` читает каждый гербер **один раз** (`read_referenced_gerbers`) и
  переиспользует байты и для набора ключей, и для `collect_entries` (карта `rel→bytes`, гербер
  изымается из неё при сборке) — двойного чтения больше нет. Вывод не меняется по смыслу, но смена
  хеша меняет значения ключей → старые артефакты в working-dir/`.cuprum` считаются устаревшими и
  перегенерируются (пользователей пока нет — допустимо). Бамп `METRICS/PREVIEW_VERSION` не нужен
  (версии хешируются как часть ключа, ширина та же).
- [x] **Показывать (почти) все хотспоты, а не топ-N** (✅ 2026-06-04). Общий потолок
  `HOT_N` поднят 40→500 (единый `pub(crate) const` в `dfm/mod.rs`, на него переведены
  sweep-дедуп `dedup_top`, clearance/width `top_n`, поясок и overshoot `truncate`) —
  на плотной плате видны все реальные нарушения, счётчик степпера честный; 500 — лишь
  backstop от патологии (молча, как решили). Дедуп по 1мм-ячейке и worst-first
  сохранены. Сверловка/via унифицированы: вместо `dedup_by_value` (до 12 на диаметр)
  — общий `cell_dedup_top` (worst-per-ячейку, кап `HOT_N`), счётчик стал той же
  природы (мёртвые после ухода drill хелперы `dedup_by_value`/`cluster_by_radius`/…
  удалены). `METRICS_VERSION` `v16`→`v17`. Грузим разом (размер не проблема: ~70 Б на
  хотспот, инфра уже возит до 4000 silk). Фронт правок не потребовал — `DrcMarkers`
  рисует чистые SVG-примитивы (тултипа на маркер нет), счётчики читают длины
  векторов. Ленивую догрузку/индикатор/виртуализацию рендера не делали (YAGNI;
  виртуализация — возможный follow-up, если на реальной плотной плате заметим лаг).
- [x] **Фильтр отображаемых типов проблем на превью** (✅ 2026-06-03). Воронка рядом с DRC-тогглом
  И правый клик по 2D-превью открывают список **актуальных** типов проблем (только те, что реально
  найдены и рисуют хотспоты — зазор/ширина/пережим/поясок/сверло/переходные/маска/шелк) с
  чекбоксами «показывать на превью». Снятая галочка прячет хотспоты этого типа из оверлея И из
  степпера «N», не трогая вердикт. Гранулярность — по «типу проблемы» (мапа `problemTypeOf`: id→
  семейство, top/bottom вместе), не по сырому `category`. Состояние эфемерное (сбрасывается при
  смене дизайна), не персистится. Заведены общие radix-примитивы `Popover`/`Checkbox`/`ContextMenu`
  (первое контекстное меню в приложении — семя паттерна).
- [x] **Читаемый overlay линейки замера на 2D-превью** (✅ 2026-06-04, PR #100). Линия и точки были
  одним белым цветом и сливались с подложкой (зелёная маска, белый шёлк, светлая медь). Теперь —
  медное ядро (`hsl(var(--primary))`) поверх тёмного кожуха `rgba(0,0,0,.6)`: линия 5/2px, концы —
  прицел-снап (кольцо + перекрестье + центр), при привязке к фиче доп. пунктирное кольцо; катеты
  ΔX/ΔY пунктиром; едва заметный скрим платы. Все размеры в экранных px (постоянны при зуме).
  `LayerStack.tsx`, дизайн-решение — в `docs/DESIGN.md`.
- ~~**Вынести CPU-bound работу фронта в воркер (не блокировать UI).**~~ **Исследовано — не делаем**
  (✅ 2026-06-04). Гипотеза была: чистые JS-деривации (`feasibility.evaluate`, вывод маркеров/issues
  в `usePreviewData`, `nest.packLayout`) на плотной плате джанкуют UI и их стоит унести в Web Worker.
  **Замер опроверг** (vitest-бенч `feasibility.bench.ts`, `pnpm bench`): на абсурдно плотной синтетике
  (500 хотспотов на семейство, 17 находок, **4502 маркера+ховера**, включая O(n²) silk-`clusterBoxes`
  по 250/сторону) полный пересчёт = **~1.4 мс в среднем / ~5 мс p999** (`evaluate` ~0.74 мс, деривация
  с реальным `i18n.t` ~0.83 мс). Это ниже бюджета кадра даже для 120 fps (8.3 мс). Воркер добавил бы
  structured-clone + async-round-trip (вероятно >1.4 мс) и **ухудшил** бы интерактивную латентность
  (markers/issues пересчитываются на каждый focus/side/filter) — ради нулевой выгоды. Тяжёлое и так в
  Rust off-thread. **SVG — отдельная история и тоже не про воркер:** наш JS лишь вставляет готовую
  строку (`dangerouslySetInnerHTML`), реальная стоимость — браузерный parse/layout/paint SVG-DOM (на
  главном потоке, но воркеру недоступен — у Worker нет DOM; разве что переписать вьювер на
  `OffscreenCanvas`). Эту стоимость уже давим уменьшением числа узлов (склейка штрихов #47–49, растровое
  превью карточки, фикс зума #46); если когда-то всплывёт зум-джанк на реальной плотной плате —
  профилировать longtask'ами в WKWebView, отдельным пунктом. Бенч оставлен как воспроизводимый артефакт.
- [x] **Динамические линейки + адаптивная сетка на холсте панели** (✅ 2026-06-04, Фаза 1). Холст
  панели получил CAD-инструментарий: edge-pinned линейки (адаптивные деления, подписи в активных
  единицах), hover-крестик + readout «X · Y», маркеры курсора, подсветка проекции бланка на шкалах,
  origin-маркер, пунктирный контур рабочей зоны станка; фикс-`CadGrid` на панели заменён адаптивной
  `AdaptiveGrid` (major совпадает с подписями). Архитектура — tech-agnostic screen-space SVG-оверлей
  `components/editor/RulersOverlay` на дескрипторе `{pxPerMm, originX, originY}` (не зависит от
  движка), чистая тик-математика в `lib/canvasTicks` (юнит-тесты). По дизайн-хэндоффу.
- [x] **Фаза 2: превью дизайна (`LayerStack`) на общий `RulersOverlay`** (✅ 2026-06-04). In-scene
  SVG-линейки заменены тем же screen-space оверлеем (дедуп рендера); inline-математика тиков
  (`STEP_LADDER`/`pickStep`/`ticksFor`/`fmtMm`) удалена в пользу `lib/canvasTicks` (сетка превью теперь
  на той же математике → совпадает с линейками). Подписи → активные единицы. Привнесён «богатый» набор
  как на панели: подсветка платы на шкалах + origin-маркер 0,0. Толщина полос линеек стала пропсом
  (`rulerTop/rulerLeft`): превью оставлено на слим-20px (без правок fit/rPad), панель — на 24/30.
- [x] **Hover-крестик сделан отключаемым** (✅ 2026-06-04). Always-on крестик + readout «X · Y» — теперь
  кнопка-тумблер (`LocateFixed`) на зум-баре обоих холстов, **по умолчанию выключен** (на превью ещё и
  скрыт в режиме замера, чтобы не спорить с измерителем). Курсор отслеживается только при включённом
  крестике (без лишних перерендеров); апдейты коалесцируются в кадр (rAF).
- [x] **Фикс артефактов крестика + взаимоисключение инструментов + ESC** (✅ 2026-06-04). Три правки
  превью/панели: (1) при включённой «Проверке» включённый крестик растеризовал тяжёлый DRC-оверлей
  (до ~500 хотспотов, включая `shape:"line"` подсветку трасс) **на каждый кадр** движения курсора — на
  сильном зуме это давало битые тонкие линии-«мусор». `projectedMarkers` мемоизирован (движение курсора
  не меняет `view` → ссылка стабильна), `DrcMarkers` обёрнут в `React.memo` → DRC не перерисовывается
  при ховере. (2) Линейка и крестик стали **взаимоисключающими**: включение одного выключает другой
  (раньше обе кнопки могли быть активны одновременно). (3) **ESC** на превью выходит из линейки (со
  сбросом точек) и гасит крестик; на панели ESC гасит крестик.

## Workstream: разбивка `crates` на пакеты

Технический долг: крупные файлы (`trace.rs` 1108, `mesh.rs` 836, `cache.rs` 683, …)
и монолитный `cuprum-core`. Цель — **и** разнести `cuprum-core` на отдельные крейты по
чистому DAG, **и** разбить большие файлы на подмодули. Ведём **один крейт = один PR**:
крейт выносится (ре-экспорт под историческим путём `pub use cuprum_X as X`, публичный API и
кеш-теги не меняются) **и его крупные файлы дробятся** на подмодули. Узел сложности — `cache`
(циклический хаб: зовёт `svg`/`dfm`/`gerber` и зовётся ими); тяжёлые крейты
(`gerber`/`mesh`/`dfm`) ждут его расщепления.

- [x] **PR1 — `cuprum-trace`** (✅ 2026-06-04, PR #73): лист без внутренних зависимостей;
  вынесен в свой крейт, `cuprum_core::trace` ре-экспортит его (вызовы в core/cli/UI не
  тронуты). Заодно сериализованы тест-кейсы глобального subscriber'а (общий `SERIAL`-мьютекс):
  изоляция в крейт вскрыла латентный флейк — `reap_idle_with_timeout` финализирует
  чужие idle-сессии при параллельном прогоне. Файл (1108 строк) разбит отдельно — см. PR2a.
- [x] **PR2a — разбить `cuprum-trace/lib.rs`** (✅ 2026-06-04): монолит 1108 строк → `config` (58,
  `CUPRUM_TRACE`-парсинг), `sink` (138, `OpSink` + visitors + registry), `layer` (121,
  routing-layer + subscriber + dispatch), `session` (код ~320 + интеграционные тесты; `operation`/
  сессии/реапер/`run_with_config`); тонкий `lib.rs` (24) с ре-экспортами. Чистое перемещение,
  публичный API не изменился. Единственный extract-PR, где файл остался монолитом (правило «и
  дробить» введено после #73).
- [x] **PR2 — `cuprum-diskcache`** (✅ 2026-06-04): `diskcache` + `artifact` вынесены в крейт
  (deps: `tracing` + `xxhash-rust`; trace — только dev). `cuprum_core::{diskcache,artifact}`
  ре-экспортят (project/UI не тронуты). `diskcache.rs` (336) разбит на `diskcache/{mod (config),
  hash, store}`.
- [x] **PR3 — `cuprum-sdcp`** (✅ 2026-06-04): протокол принтера (discovery/client/upload) вынесен
  в крейт; ноль внутренних зависимостей. `cuprum_core::sdcp` ре-экспортит (CLI/UI не тронуты).
  `reqwest` + `tungstenite` + `md-5` + `uuid` убраны из `cuprum-core` (их использовал только sdcp).
  Файлы уже модульные и мелкие (≤186) — разбивать нечего.
- [x] **PR4 — разорвать циклы `cache`-хаба** (✅ 2026-06-04): `parse_layer_cached` (+ parse-кеш и
  тесты) перенесён из `cache` в `gerber` — он был **корнем всех циклов** (svg/geometry/mesh/dfm
  зависели от cache **только** ради него). Теперь они зовут `gerber::parse_layer_cached`; `cache`
  имеет только исходящие рёбра (→ svg/gerber/dfm), от него зависят лишь вышестоящие `preview`/
  `compose`. DAG модулей ацикличен → тяжёлые крейты разблокированы. Чистое перемещение, вывод и
  кеш-теги не тронуты.
- [x] **PR4b — растащить result-обёртки по доменам** (✅ 2026-06-04): svg-кеш-обёртки
  (`layer_svg_cached`/`layer_svg_artifact`/`svg_artifact_key` + статики + тесты) → `svg`;
  metrics-обёртки (`board_metrics_cached`/`board_metrics_artifact`/`metrics_artifact_key`) → `dfm`.
  Движок single-flight (`cached_single_flight`/`_persistent`) стал `pub(crate)` и остался в `cache`;
  `cache` теперь = path+mtime-кеши (`preview_png`/`native_mask`) + движок + **фасад**, ре-экспортящий
  перенесённые обёртки под историческими `cache::`-путями (project/UI/preview не тронуты). Чистое
  перемещение, вывод и кеш-теги не тронуты. (preview_png/native_mask — это и есть исходная суть
  `cache.rs` «path+mtime render cache», оставлены на месте.)
- [x] **PR5 — вынести движок single-flight в лист-крейт `cuprum-cache`** (✅ 2026-06-04):
  generic-движок (`cached_single_flight`/`_persistent` + private `_with` + тест) → новый
  лист-крейт `cuprum-cache` (зависит только от `cuprum-diskcache`), функции стали `pub`.
  Вызовы в `svg`/`dfm` переключены на `cuprum_cache::…`. `cache.rs` в core теперь = только
  path+mtime-кеши (`preview_png`/`native_mask`, зависят от `gerber`) + фасад. Это пререквизит
  выноса тяжёлых крейтов: `svg`/`dfm`/`gerber` зовут движок, а core зависел бы от них при выносе.
  Чистое перемещение, вывод и кеш-теги не тронуты.
- [x] **PR6 — вынести `goo` в лист-крейт `cuprum-goo`** (✅ 2026-06-04): `.goo`-кодирование +
  геометрия экрана (`SCREEN_*`/`SCREEN_PX_PER_MM_*`) → новый лист-крейт `cuprum-goo` (deps:
  внешний `goo` + anyhow + tracing). В core — фасад `pub use cuprum_goo as goo;`, поэтому
  `cuprum_core::goo::…` (CLI, UI, gerber, compose) резолвится без правок вызовов. Снимает
  ребро `gerber → goo` перед выносом `cuprum-gerber`. Чистое перемещение, вывод не тронут.
- [x] **PR7 — вынести `cuprum-gerber` + разбить `geometry.rs`** (✅ 2026-06-04): модули
  `gerber`/`svg`/`geometry`/`strokes`/`drill` → новый крейт `cuprum-gerber` (deps:
  `cuprum-cache`+`cuprum-goo`+`cuprum-diskcache` + gerber_viewer/tiny-skia/png/lru/serde/
  i_overlay/tracing). В core — фасад `pub use cuprum_gerber::{drill,geometry,gerber,strokes,svg};`,
  так что `cuprum_core::{…}::…` (CLI, UI, cache/mesh/dfm/compose/preview) резолвится без правок
  вызовов; внутри группы `crate::goo`→`cuprum_goo`, `crate::{diskcache,artifact}`→`cuprum_diskcache::…`.
  6 измерительных функций geometry (`point_*`/`poly_containing`/`circle`/`shapes_to_polys`) повышены
  `pub(crate)`→`pub` (их зовёт core). Заодно `geometry.rs` (683) разбит на `geometry/{mod,build,tess,measure}.rs`.
  Чистое перемещение, вывод не тронут.
- [x] **PR8 — вынести `cuprum-mesh` + разбить `mesh.rs`** (✅ 2026-06-04): модуль `mesh` →
  новый крейт `cuprum-mesh` (deps: `cuprum-gerber`+`cuprum-trace` + gerber_viewer/i_overlay/
  earcutr/rayon/tracing). В core — фасад `pub use cuprum_mesh as mesh;`, так что
  `cuprum_core::mesh::…` (UI, preview, dfm) резолвится без правок вызовов; внутри переписаны
  `crate::{geometry,gerber,drill}`→`cuprum_gerber::…`, `crate::trace`→`cuprum_trace`. `outline_info`
  повышена `pub(crate)`→`pub` (зовёт preview/dfm). Из core убраны ставшие неиспользуемыми
  `earcutr`/`i_overlay`. Заодно `mesh.rs` (836) разбит на `mesh/{lib,emit,outline,build}.rs`.
  Чистое перемещение, вывод не тронут.
- [x] **PR9 — вынести `cuprum-dfm` + разгрузить `metrics/mod.rs`** (✅ 2026-06-04): весь каталог
  `dfm/` → новый крейт `cuprum-dfm` (deps: `cuprum-cache`/`cuprum-gerber`/`cuprum-mesh`/
  `cuprum-diskcache`/`cuprum-trace` + serde/serde_json/lru/rayon/gerber_viewer/tracing). В core —
  фасад `pub use cuprum_dfm as dfm;`, так что `cuprum_core::dfm::…` (CLI, UI, cache-фасад)
  резолвится без правок вызовов; внутри переписаны `crate::dfm::`→`crate::`,
  `crate::{geometry,gerber,drill}`→`cuprum_gerber::…`, `crate::mesh`→`cuprum_mesh`,
  `crate::trace`→`cuprum_trace`, `crate::{diskcache,artifact}`→`cuprum_diskcache::…`. Из core
  убраны ставшие неиспользуемыми `gerber_viewer`/`lru`/`serde`/`serde_json`/`cuprum-cache`.
  `metrics/mod.rs` (516, из них ~460 — тесты) разгружен: тело тестов → `metrics/tests.rs`
  (mod.rs стал 54 строки). Чистое перемещение, вывод не тронут.

**Итог кампании:** монолит `cuprum-core` распался на **11 крейтов** (core + trace/diskcache/
cache/goo/sdcp/gerber/mesh/dfm + cli/project), DAG ацикличен. `cuprum-core` теперь = только
оркестрация: `compose` (растеризация+печать), `preview` (path→PNG), `cache` (path+mtime
render-кеши + фасады), `cal`. Крупные файлы разбиты (trace 1108, diskcache, geometry 683,
mesh 836, metrics 516). Публичный API и кеш-теги ни в одном PR не тронуты — всё через фасады
`pub use cuprum_X …` под историческими путями.

### Форк `gerber-viewer` → ядро парсинга в `cuprum-gerber`

- [x] **Фаза A — форк-и-обрезка вендоренного `gerber-viewer`** (✅ 2026-06-04, PR #120):
  вендоренный crate `vendor/gerber-viewer/` (6206 строк, рассчитан на egui-рендер) форкнут до
  ядра парсинга Gerber→примитивы и влит в `cuprum-gerber` как модуль `src/viewer/`. Выкинут весь
  egui-рендер (`renderer`/`ui`/`drawing`/`color`), `lyon`-тесселяция (`geometry/mesh.rs` + поле
  `tessellation`), все `#[cfg(feature="egui")]`-элементы, и ставшая мёртвой render-математика
  (`GerberTransform` + render-методы `BoundingBox` + весь `mirroring.rs`; `transform.rs` 952 →
  влит в `geometry/mod.rs` как ~90-строчный `GerberImageTransform`). Зависимости egui/lyon/rand/
  profiling убраны; остались `nalgebra`/`gerber_types`/`gerber_parser`/`thiserror`/`log`.
  `vendor/`-дерево удалено, `exclude` в корневом `Cargo.toml` почищен. `cuprum-gerber` реэкспортит
  `GerberLayer`/`GerberPrimitive`/`Exposure` + `gerber_parser`/`gerber_types`, так что `cuprum-mesh`/
  `cuprum-dfm` поменяли только `use gerber_viewer::…`→`use cuprum_gerber::…` — публичный API наружу и
  кеш-теги не тронуты. Атрибуция форка (MIT/Apache, NOTICE + LICENSE-*) на месте. Ядро: 6206 → 3859
  строк; тесты ядра viewer'а теперь гоняются в воркспейсе (+47 нетто к набору).
- [ ] **Фаза B — разбить `viewer/layer.rs`** (2501 строки — крупнейший файл): на фокусные модули
  `primitive`/`bbox`/`build`/`layer` (типы примитивов / `WithBoundingBox` / билдер `GerberLayer` +
  `Region`/`ApertureKind` / структура + аксессоры). Чистый перенос, поведение/число тестов не меняются.
- [ ] **Техдолг: `GerberImageTransform` — полу-разведённая фича.** Команды MI/SF/OF/IR/AS парсятся
  (`build_image_transform`) и хранятся в `GerberLayer.image_transform`, но `to_matrix()` нигде не
  вызывается → трансформ **не применяется** к координатам примитивов: эти (deprecated) Gerber-команды
  молча игнорируются при рендере/измерениях. Решить: либо применить трансформ в билдере, либо явно
  убрать поле/аксессор и задокументировать как неподдерживаемые. (Найдено при форке gerber-viewer.)

## Workstream: переосмысление CLI с нуля

`cuprum-cli` — древнейший крейт, с него начинался проект, и он морально устарел:
один `main.rs` (~629 строк) с одношотовыми командами над **одиночными `.gbr`-файлами**
(`discover` / `gerber-info` / `render` / `prepare` / `print` / `calibrate` / `gen-goo` /
`upload` / `stop` / `expose`). Всё это написано **до** модели документа `.cuprum`, панели
(`BoardInstance`/`ToolingHole`), персистентных артефактов, DFM и CNC-ветки — CLI ничего из
этого не знает и оперирует понятиями «один гербер → одна засветка». По сути это тупиковый
прототип, а не инструмент. **Решение: выкинуть и спроектировать заново с нуля.**

- [x] **Брейншторм нового CLI (спека)** (✅ 2026-06-04). Согласовано: CLI — **тулбокс**
  эргономичных команд поверх движка, вытаскивающий наружу возможности core (герберы/проект →
  SVG/PNG, DFM, 3D-меш, инспект), а не CI-гейт вокруг документа. Вход — **и сырые герберы, и
  `.cuprum`**. v1-набор: `info`/`render`/`svg`/`3d`/`check`; железо и мутации проекта — позже.
  Архитектура: тонкий CLI, общая склейка едет вниз в библиотеки (никакой параллельной логики).
- [x] **Переписать `cuprum-cli` с нуля** (✅ 2026-06-04, PR #116/#117/#118/#119) — v1-тулбокс
  `info`/`render`/`svg`/`3d`/`check` над папкой герберов и `.cuprum`. По фазам, PR на фазу:
  - [x] **Фаза 0 — фундамент** (✅ 2026-06-04, PR #116): `role_side` вынесен в
    `cuprum-project::layer` (общий с UI); `resolve_design` (`cuprum-project::resolve`) сводит
    папку герберов / `.cuprum` к единому `ResolvedDesign`; новый clap-каркас + команда `info`;
    старый `main.rs` (~629 строк железных команд) удалён целиком.
  - [x] **Фаза 1** — `render` (композитный PNG) + `svg` (композитный SVG) (✅ 2026-06-04, PR #117).
  - [x] **Фаза 2** — `3d`: сериализаторы меша (glTF/STL/OBJ) в `cuprum-mesh` + команда (✅ 2026-06-04, PR #118).
  - [x] **Фаза 3** — `check`: измеренные DFM-метрики + минимальный гейт (exit-код) (✅ 2026-06-04, PR #119).
  - [ ] **Дальше (follow-up):** полный паритет DFM-вердикта (порт `feasibility.ts` в общий
    Rust-`dfm::verdict` + переключение GUI); bottom-side/per-layer для render/svg; растровый
    3D-снимок; железная фаза (compose→`.goo`, discover/upload/expose) и мутации проекта.

## Phase 1 — Panel-модель `[in progress]`

**Цель:** перевести модель данных с `placements[]` на схему из
[`PROJECT.md`](PROJECT.md), чтобы остальное строилось на ней, а не на
обходных путях.

- [x] Schema migration: `placements[]` → `Panel + BoardInstance[] + ToolingHole[]` (✅ 2026-06-01, [PR #12](https://github.com/fixcik/cuprum/pull/12) рефактор `document/` + централизованные миграции, [PR #13](https://github.com/fixcik/cuprum/pull/13) структуры + схема v5)
- [x] Переименование `manifest.imports[]` → `manifest.designs[]` (✅ 2026-05-31, PR #4)
- [x] `Stackup` в манифесте: `copper_weight_oz`, `substrate_thickness_mm`, `double_sided` (+ `panel` в манифесте, схема v4) (✅ 2026-05-31, PR #4)
- [ ] Panel editor UI: холст в Panel space, drag BoardInstance, инструмент «ToolingHole»
      (сейчас есть только редактор FR4-бланка: размер + stackup)
  - **Добавление дизайна на панель** — отдельное OS-окно (Tauri `WebviewWindow`): выбор
    дизайна проекта + поиск + импорт ZIP + авторазмещение. Главное окно владеет состоянием,
    окно шлёт интенты (import / add-to-panel); результат — `BoardInstance` в `panel.instances`.
    Ведём фазами:
    - [x] Фаза 1 — окно + мост состояния (интенты) + палитра инструментов на холсте +
          добавление одной платы «плотно в угол» + минимальный read-only рендер instance'ов
          (✅ 2026-06-03, [PR #53](https://github.com/fixcik/cuprum/pull/53))
    - [x] Фаза 2 — авторазмещение (nesting): настройки (`nest` в settingsStore, persist) + чистый
          grid-packer `packLayout` + живое превью «На панели» (сегмент Дизайн/На панели) + строка
          `fitLine` с переполнением; `addBoardInstances` кладёт массив копий, `nest` едет в payload
          интента (главное окно не читает стор) (✅ 2026-06-03, [PR #61](https://github.com/fixcik/cuprum/pull/61))
    - [x] Фаза 3 — схематичное превью «Дизайн» в окне (`LayerStack` без маски, 2D/3D, Верх/Низ,
          «Проверка») на новом хуке `usePreviewData` (загрузка SVG/меш/метрик + деривация находок);
          окно read-only (без правки слоёв/flush), маска исключена в 2D (layers) и 3D (visibleKeys)
          (✅ 2026-06-04, [PR #69](https://github.com/fixcik/cuprum/pull/69))
    - [x] **Конвергенция `DesignInspector` на `usePreviewData`** (✅ 2026-06-04,
          [PR #81](https://github.com/fixcik/cuprum/pull/81)). Инспектор переписан как потребитель
          общего хука; удалена inline-копия load+derive (~466 строк), дублировавшаяся после выноса
          инспектора в отдельное окно (#70). Хук получил два обратносовместимых расширения: опц.
          `hiddenTypes` (фильтр типов проблем в `markers`/`issues`) и экспорт `isVisible` (строки
          редактируемого списка слоёв). `focusNonce` приведён к `useState` как в окне add-design.
          Оба окна (add-design + inspector) теперь на одном движке.
    - [x] **Интерактивный редактор размещённых дизайнов на панели** (✅ 2026-06-04, PR #87/#92/#94).
          Ручное управление `BoardInstance`'ами на холсте: **выделение** (клик + Shift/Ctrl + marquee),
          **перемещение** (drag, кламп в панель + снап 1 мм, Alt — свободно), **свободный поворот**
          вокруг центра с привязкой к градусам (ручка-кноб, 15° / Shift-Alt → 1°), **Дублировать**
          (Ctrl+D) / **Удалить** (Delete) — по одному и массово; хоткеи (Esc/Ctrl+A/стрелки). Единая
          центр-пивот модель позы в packer/canvas/bounds (`instanceBounds` → настоящий повёрнутый AABB);
          эфемерный `panelSelectionStore`; мутации через `savePanelConfig` (один шаг undo, автосейв).
          Отгружено 3 PR: #87 геометрия+рендер, #92 выделение/move/delete, #94 поворот+дубль.
      - [x] **Точность клампа** (✅ 2026-06-04, PR #101). Нудж стрелками и офсет дубля доведены до
        точного повёрнутого AABB-клампа (`boxesForInstances` + `clampDeltaToPanel`) — как drag.
        Габариты плат вынесены в общий хук `usePlacedBoardSizes` (фетч-и-прун был в двух копиях —
        `PanelEditor` и `PanelBlankCanvas`). `duplicateInstances(ids)` → `(ids, dxMm, dyMm)`: офсет
        копий считает и клампит вызывающий (store «тупой», как `moveInstances`); на заполненной панели
        офсет схлопывается в 0 (копия ложится на оригинал).
      - [x] **Контекстное меню (правый клик) по инстансу/выделению на холсте** (✅ 2026-06-04, PR #105).
        Пункты: Дублировать · Повернуть 90° ↻/↺ · Сбросить поворот · Удалить — те же эффекты, что
        палитра/хоткеи; `disabled` при пустом выделении. Контейнер канваса обёрнут в Radix
        `ContextMenuTrigger asChild` (ловит нативный `contextmenu` с `<canvas>`); правый клик по
        инстансу сперва выделяет его. Поворот/сброс переиспользуют общий «rotate + кламп-назад»
        (`clampSelectionIntoPanel`). «Переразместить» отложено — re-nest ещё нет (см. ниже).
      - [x] **«Открыть дизайн» в контекстном меню инстанса** (✅ 2026-06-04, PR #121). Открывает
        инспектор дизайна (`design_id` выделенного инстанса) в отдельном окне (`api.openInspectorWindow`),
        как кнопка «Открыть» на карточке дизайна. Пункт первым в меню (сепаратор перед действиями
        редактирования), активен при ровно одном выделенном инстансе.
      - [x] **Выравнивание/распределение выделения (align bar)** (✅ 2026-06-04, PR #107).
        Плавающая панелька снизу по центру, появляется при ≥2 выделенных: align по краям/центру
        (6) к bbox выделения + distribute по H/V (2, при ≥3). Геометрия — чистые тестируемые
        `alignInstances`/`distributeInstances` на повёрнутых AABB; стор получил «тупой» батч-сеттер
        поз `setInstancePoses` (один undo).
      - [x] **Умные направляющие при drag (магнитная привязка)** (✅ 2026-06-04, PR #111).
        При перетаскивании выделение притягивается к краям/центрам других инстансов и панели
        (рамка `{0,0,W,H}` как цель → края+центр панели); magenta-пунктир `#ff4d9d`, порог
        `SNAP_PX=6` экранных px, Alt отключает. Геометрия — чистая тестируемая `computeSmartGuides`
        на повёрнутых AABB; оси без привязки падают на grid 1 мм, кламп — после.
      - **Дальше (полировка после v1 редактора):** «Переразместить» (повторный nesting только
        выделенных), tooling-инструмент (размещение `ToolingHole`). Корректный растр `compose`
        под произвольный угол (анизотропные пиксели) — см. пункт «compose рефактор» ниже.
    - [x] **Единицы в редакторе панели/nesting** (✅ 2026-06-04). У `UnitField` появился проп `dim`
          (`coarse` мм/дюйм, `fine` мм/мил): при заданном `dim` поле гоняет `toDisplay`/`fromDisplay`,
          подставляет суффикс через `unitLabel` и подбирает шаг под активную единицу — модель остаётся в
          мм. Поля размера/толщины бланка (`PanelEditor`) и зазоры/край/шаг (`NestingControls`) переведены
          на `dim`; «, мм» убрана из подписи группы зазоров, литерал «мм» — из хинта `maxFromSettings`
          (единицу несёт само значение, `fmtLen`). В имперском режиме теперь конвертируются.
    - [x] **Редизайн настроек панели → правый док-инспектор** (✅ 2026-06-04, [PR #114](https://github.com/fixcik/cuprum/pull/114)).
          Параметры панели переехали из левой колонки `w-80` в правый док `PanelInspector`: сворачивается в
          рельс 44px, ресайзится (280…480px), аккордеон-секции «Размер»/«Заготовка» — UI-состояние персистится
          (слайс `settingsStore.panelInspector`). Шапка-сводка (W×H + бейдж рабочей зоны + amber-алерт превышения),
          чипы-пресеты (мини-SVG, active по совпадению размера+стэкапа) + «Сохранить как пресет», «Повернуть 90°»,
          живая схема разреза заготовки `StackupDiagram` (реагирует на стороны/медь/толщину; чистый `lib/stackup`).
          Канвас стал героем (центр). Секцию «Авторазмещение» решили не добавлять (параметры упаковки задаются
          в окне добавления дизайна).
- [x] **(баг) Настройки панели не сохраняются** (✅ 2026-06-03). Регресс пережил рефактор на
      working-dir: правка панели шла обсолетным путём `configure_panel` (прямая запись в
      контейнер `.cuprum` + зеркалирование loose-манифеста без репака), мимо общего
      `_persistManifest`. Сериализованный репак working-dir (артефакт-флуш на открытии или
      другая мутация) перепаковывал loose-манифест без панели поверх контейнера — правка
      терялась к реоткрытию. Фикс: `savePanelConfig` идёт через тот же working-dir + autosave
      путь, что и все мутации; обсолетный `configure_panel` (Tauri-команда + core-функция)
      удалён.
- [x] **(баг) «N дизайнов выходят за край панели» врёт** (✅ 2026-06-04, PR #77). Реальная
      причина оказалась НЕ в work-area (гипотеза была неверной), а в модели поворота: `instanceBounds`
      (#62) считала bbox свободным поворотом вокруг origin (Konva-математика), тогда как размещение —
      осе-выровненный footprint в `(x_mm,y_mm)` со swap w↔h для 90°/270° (так кладёт `packLayout` и
      рисует `PanelBlankCanvas`, без Konva-поворота). Для 90° старая формула давала `minX = x − boardH
      < 0` → ложное «выходит за край» у всех повёрнутых при нестинге инстансов. Фикс: `instanceBounds`
      переведена на ту же осе-выровненную модель (swap для 90°/270°, bbox `[x,x+fw]×[y,y+fh]`); vitest
      расширен регрессом. Произвольные углы (будущий интерактивный редактор) потребуют настоящего
      повёрнутого bbox — помечено комментарием.
- [x] **(баг) «Дизайн больше рабочей зоны панели» при выключенном авторазмещении** (✅ 2026-06-04,
      PR #89). В окне add-design плата, влезающая в контур панели (228×98 на 240×200), отбраковывалась
      («tooBig», пустое превью «На панели»), если отступ-«Край» (`nest.marginMm`) был увеличен: `packLayout`
      применял «Край»/зазор даже при выключенном nesting, и `innerW = 240 − 2·10 = 220 < 228` давал ноль
      колонок. Фикс: «Край»/зазор — параметры авторазмещения; при выключенном nesting («плотно в угол»)
      `margin`/`gap` = 0 и в расчёте вместимости, и в якоре — одиночная копия садится впритык в угол и
      размещаема всегда, когда плата влезает в сырой контур. vitest расширен регрессом.
- [ ] **Запретные зоны на панели (keep-out).** Области панели, куда нельзя класть
      `BoardInstance`/`ToolingHole` (зажимы станка, поля под реперы, кромка раскроя).
      Зона задаётся в Panel space; размещение в ней — DFM-находка/блок в редакторе.
- [ ] `compose` рефактор: Panel space → Machine space (UV-пиксели); произвольный
      поворот инстанса требует анизотропно-корректного ре-рендера в
      `compose::compose_layout` (там TODO — пиксели 14×19 мкм)
- [x] Backward compat: открытие старых `.cuprum` мигрирует автоматически (✅ 2026-06-01,
      [PR #12](https://github.com/fixcik/cuprum/pull/12) — централизованный Value-пайплайн
      `migrate`, [PR #13](https://github.com/fixcik/cuprum/pull/13) — шаг v4→v5)

**Milestone:** старый проект открывается, виден Panel вместо LCD,
добавляются ToolingHole'ы, засветка работает как раньше.

## Phase 2 — CNC workflow `[next]`

**Цель:** первый практический путь до платы без химии — isolation milling
на CNC 3018. Это самый быстрый способ дойти до реального результата и
откалибровать сам станок.

- [ ] `MachineProfile` для CNC: расширение `capabilityProfile` до варианта
      `Cnc { spindle, runout, backlash, probe, gcode_dialect, ... }`.
      Дефолт под CNC 3018 Lunyee.
- [ ] `ToolLibrary` в settings: `Drill / EndMill / VBit` с диаметром,
      углом (для VBit), `max_rpm`, рекомендованными feeds для FR4
- [ ] **G-code emitter** для GRBL 1.1:
      - G0/G1, M3/M5, S для шпинделя
      - G38.2 для probe
      - M6 как manual pause для смены инструмента
- [ ] **Isolation milling**: `isolation_paths(layer, tool, depth)` в
      `geometry.rs` — offset polygon на ширину реза V-bit; multi-pass для
      voronoi clearing
- [ ] **ProfileRoute** с авто-Tab'ами по периметру + drag для коррекции
- [ ] **Auto-leveling**:
      - probing UI (зажим-крокодил к меди + щуп)
      - G38.2 grid по сетке N×N
      - построение HeightMap
      - warp-компенсация Z на этапе эмиссии G-code
- [ ] **Калибровка станка.** Механическая калибровка CNC отдельно от auto-leveling:
      steps/mm по осям, backlash-компенсация, выставление нуля/origin, проверка
      перпендикулярности. Складывается в `MachineProfile` (Cnc) и переиспользуется
      эмиттером G-code. (Калибровка UV-дозы — отдельно, Phase 4: StepExposureTest +
      CalibrationLog.)
- [ ] **Редактор ProcessStep'ов**: массив, drag для переупорядочки, кнопки
      «Сгенерировать G-code» и «Сбросить к шаблону»

**Milestone:** сгенерил `.nc`, прогнал по воздуху в Candle/UGS, движения
адекватные.

## Phase 3 — Первая реальная плата

**Цель:** выход из теории. Без этого следующие фазы — пальцем в небо.

- [ ] Купить материалы:
      - односторонний FR4 100×100 мм (1.6 мм)
      - V-bit 30° (1–2 штуки)
      - карбидные свёрла 0.6 / 0.8 / 1.0 мм
      - endmill 1 мм PCB-router (chipbreaker)
      - spoil board MDF
      - зажим-крокодил для probe
- [ ] Допилить probe UI (после прогона на железе всплывут косяки)
- [ ] Развести в KiCad простую плату — LED-мигалка на NE555 или
      что-то аналогичное с DIP-компонентами
- [ ] Прогнать весь pipeline: импорт → Panel → ToolingHole → ProcessStep'ы
      → milling → drill → ProfileRoute
- [ ] Собрать → проверить мультиметром → запаять компоненты → завестись

**Milestone:** одна **работающая** плата в руках. Из неё — список багов и
приоритетов на Phase 4.

## Phase 4 — Доделать UV-flow на новой модели

**Цель:** доказать параллельный путь — UV-засветка тоньше и быстрее
milling'а на сложных платах.

- [ ] `compose` рефактор: читает Panel + BoardInstance + LayerRef,
      mirror по Y для `Bottom`
- [ ] **StepExposureTest** — мульти-слойный `.goo` для калибровки дозы
- [ ] **CalibrationLog** базовый: запись результата + история, разворот
      под текущим временем засветки в `MachineProfile`
- [ ] Двусторонка на ToolingHole: засветка top → переворот → засветка bottom

**Milestone:** первая двусторонка через UV-засветку.

## Phase 5 — Полировка (в порядке убывания пользы)

- [ ] **Compensation** (default per GerberLayer) + UI-слайдер с live preview
- [ ] **Gerber X2** парсинг (`AperFunction`), fallback на эвристику
- [ ] **Solder mask** workflow (отдельная плёнка, засветка, проявка)
- [ ] Дополнительные TestPattern'ы: `TraceWidthTest`, `ClearanceTest`,
      `AnisotropyTest`, `MaskDoseTest`
- [ ] Silkscreen
- [ ] Поддержка других UV-LCD принтеров (новые `MachineProfile`)
- [ ] **Меднение / PTH** — графитовая активация + CuSO4 электролиз, если
      реально пойдёшь

## Backlog `[не делать пока]`

Сюда то, что **не** в плане ближайшего года:

- Multilayer (4+ слоя) — дома практически невозможно
- Pattern plating (требует tin-ванну и второй цикл резиста)
- Surface finish (ENIG / HASL / OSP) — промышленный процесс
- DRC / netlist-валидация — это работа KiCad, не Cuprum
- Импорт чего-либо кроме Gerber + Excellon (ODB++, IPC-2581)
- Электрический тест (ICT, flying probe)

## Открытые вопросы по приоритетам

1. **VISION.md под новый порядок.** Сейчас он говорит «UV первичен»,
   milling вообще не упомянут. Надо обновить, чтобы документация не
   рассинхронилась.
2. **Phase 3 — какая плата?** LED-мигалка на NE555 или сразу что-то
   утилитарное под себя (например, breakout-плата под датчик)?
3. **Phase 2 — auto-leveling: интегрировать в Cuprum или сначала
   использовать сторонний инструмент** (bCNC AutoLevel, AutoLeveller AE)
   и вшить в Cuprum только после первой платы?

---

*Ревизия 2026-06-04. Обновлять при сдвиге фаз и появлении новых
приоритетов.*
