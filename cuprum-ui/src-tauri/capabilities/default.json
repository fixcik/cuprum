{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for app windows",
  "windows": ["main", "add-design"],
  "permissions": [
    "core:default",
    "dialog:default",
    "core:window:allow-set-title",
    "core:window:allow-close"
  ]
}
